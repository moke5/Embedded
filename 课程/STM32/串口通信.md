# 通信

[toc]



## 1 通信概述

### 1.1 通信的概念

  通信是当代计算机应用的核心。由单个计算机构成的封闭系统有很大的局限性：能力不足、系统复杂、实时性差、系统封闭，不易于产品兼容；信息不能被共享，重复而造成浪费等。基于上述原因，多计算机系统出现。

  在单片机的世界则称之为多机通信，**多机通信是指两台以上的单片机组成的网络结构**，可以通过串行通信方式实现数据交换和控制。多机通信的网络拓扑结构有星型、环型和主从式多种结构，其中以主从式结构应用较多。该结构系统中，一般有一台主机和多台从机。主机发送的信息可以传送到多台从机或指定从机，而从机发送的信息只能传送到主机，各从机之间不能直接通信。

![img](./img/wKgP3GIpyhaABlmYAACxZGKARJE435.png)

## 2 **信息传送的方式**

  计算机通信根据信息传送方式可分为**并行和串行通信**。

串行通信接口（Serial Communication Interface, SCI），简称串口，是一种**按位顺序发送数据**的通信接口。与并行通信相比，串行通信只需要较少的信号线，适合于远距离通信。串口通信一般分为两种类型：异步串行通信（如RS-232）和同步串行通信（如SPI、I²C等）。

**并行通信**

  两通信设备一次交换多个bit的数据的通信方式。其传输的速率用每秒钟传送的位数来表示（b/s）。其特点是通信速率高，缺点是通信线路多，因为每一bit的数据都要有一条数据线与之对应，不适用于远程通信。并行传输的一个例子是并行接口打印机。

![img](./img/wKgP3GIpyhaATu1aAAD2aSsvIpc863.png)

**串行通信**

  两通信设备一次交换一个bit的数据的通信方式，即字符数据（码字）是逐位（码元）传送的，一个字符由若干位组成。
  特点是通信速率较低，但通信线路少，经济，可靠，适用于远程通信。

![img](./img/wKgP3GIpyhaAeNw5AACn6DKE25o830.png)

## 3 **通信的工作方式**

**单工方式**

  数据只能从一方传向另一方，而不能往反方向传输。这是根据应用的特点，对通信双方的软、硬件进行了简化。单向传输设备，如并行接口打印机就是通过单工的方式进行数据传输的。

如：打印机

![img](./img/wKgP3GIpyhaANDdFAABLOO6S0Jg160.png)

**半双工方式**
  允许数据往两方传向传输，但只能交替进行，而不能同时进行的传输方式。常见应用如对讲机。

![img](./img/wKgP3GIpyhaAEouHAACPRkvziHU190.png)

**全双工方式**
  数据可同时往两方向传输的传输方式。电话是全双工的典型例子。

![img](./img/wKgP3GIpyhaAV-JlAACBQ3y3kXA870.png)

## 4 **同步通信与异步通信**

### **4.1 同步方式**

在同步通讯中，收发设备双方会**使用一根信号线表示时钟信号**，在时钟信号的驱动下双方进行协调 。 通讯中通常双方会统一**规定在时钟信号的上升沿或下降沿对数据线进行采样**。

![img](./img/56811.png)

  同步方式是一种连续传输若干字符的串行通信方式。通信时，收发双方的码字（字符或字节）和码元（位）有严格的时序关系。

![img](./img/wKgP3GIpyhaAdpSdAAECRMaHFL4221.png)

  码字中的各个码元之间的同步称为位同步；码字之间的同步称为群同步。单片机常用的同步通信有I2C、SPI等。

**SPI时序图**

![img](./img/wKgP3GIpyhaAQUL_AAGH07opQlE041.png)

**I2C时序图**

![img](./img/wKgP3GIpyhaARZJQAAGtwjz6TY4438.png)

### **4.2 异步方式**

在异步通讯中不使用时钟信号进行数据同步，它们直接在数据信号中穿插一些同步用的信号位，或者把主体数据进行打包， 以数据帧的格式传输数据。**某些通讯中还需要双方约定数据的传输速率，以便更好地同步。**

![img](./img/56812.png)

  异步通信时收发双方在码元和码字上都不存在严格的时序关系。其特点是：字符（码字）是一帧一帧的传送，每一帧的传送由起始位开始，以停止位结束。接收方通过对起始位和停止位的检测与发送方同步，因此也称为起止式异步通信。

  与同步通信不同，异步通信中，发送方和接收方有各自的时钟，一帧传输一个字符。每帧由起始位（规定为逻辑0）、数据位（5～8位）、奇偶校验位、停止位（规定为）组成，起止位的作用是使通信双方同步。

![img](./img/wKgP3GIpyhaAKMsKAAEpw9HpEM4544.png)

**同步通信与异步通信方式的比较**

| 方式 |    信息传送    |   串行时钟   |  同步依据  |
| :--: | :------------: | :----------: | :--------: |
| 同步 |   连续不间断   |   共用一个   |  同步信号  |
| 异步 | 以帧为单位间断 | 各自独立时钟 | 起、止信号 |

## **5.通讯速率**

通常用比特率(Bit rate)或波特率(Baud rate)，衡量通讯速率。

**1. 比特率**

**比特率：**单位时间内传输的二进制数据位数。

**单位：**比特每秒（bit/s，bps），常用衍生单位：kbps（千比特/秒）、Mbps（兆比特/秒），如网络带宽100Mbps以太网

 

**2. 波特率**

**波特率：**单位时间内传输的信号码元（调制符号）个数

**单位：**波特（Baud），1波特表示每秒传输1个码元。

**码元：**如果在通讯传输中，有0V、2V、4V以及6V分别表示二进制数00、01、10、11， 那么每个码元可以表示四种状态。 如果通讯传输中，用0V表示数字0，5V表示数字1，那么一个码元可以表示两种状态0和1，所以一个码元等于一个二进制比特位， 此时波特率的大小与比特率一致。

 一个 M 进制码元携带比特数：

$$\boldsymbol{k=\log_2 M}$$

- M：码元有多少种可区分信号状态。
- 比特率 = 波特率 × $\log_2M$。



---

# 串口通信

## 1 串口通信

### 1.1 历史背景

  串口的出现是在1980年前后，数据传输率是115kbps～230kbps。串口出现的初期是为了实现连接计算机外设的目的，初期串口一般用来连接鼠标和外置Modem以及老式摄像头和写字板等设备。串口也可以应用于两台计算机（或设备）之间的互联及数据传输。由于串口（COM）不支持热插拔及传输速率较低，部分新主板和大部分便携电脑已开始取消该接口。串口多用于工控和测量设备以及部分通信设备中。

**常见的公口与母口**

![img](./img/wKgP3GIpyieAClSbAAOFTa9GoQs914.png)

**串口应用领域**

![img](./img/wKgP3GIpyieAAbE0AASfucLDnTs775.png)

### 1.2 定义

  **串行接口 (Serial Interface)** 是指数据一位一位地顺序传送，其特点是通信线路简单，只要一对传输线就可以实现双向通信（可以直接利用电话线作为传输线），从而大大降低了成本，特别适用于远距离通信，但传送速度较慢。一条信息的各位数据被逐位按顺序传送的通讯方式称为串行通讯。串行通讯的特点是：数据位的传送，按位顺序进行，最少只需一根传输线即可完成；成本低但传送速度慢。串行通讯的距离可以从几米到几千米；根据信息的传送方向，串行通讯可以进一步分为单工、半双工和全双工三种。

![img](./img/wKgP3GIpyieAJ-rnAAGlc4eUwIc260.png)

### 1.3 接口划分标准

  **同步串行接口（Synchronous Serial Interface，SSI）**是一种常用的工业用通信接口。

  **异步串行是指UART（Universal Asynchronous Receiver/Transmitter）**，通用异步接收/发送。UART是一个并行输入成为串行输出的芯片，通常集成在主板上。UART包含TTL电平的串口和RS232电平的串口。 TTL电平是3.3V的，而RS232是负逻辑电平，它定义+5 ~ +12V为低电平，而-12~-5V为高电平，MDS2710、MDS SD4、EL805等是RS232接口，EL806有TTL接口。

> USART:通用同步异步收发器，简称串口
>
> Universal Synchronous/Asynchronous Receiver/Transmitter

  串行接口按电气标准及协议来分包括RS-232-C、RS-422、RS485等。RS-232-C、RS-422与RS-485标准只对接口的电气特性做出规定，不涉及接插件、电缆或协议。

**1、RS-232**

  也称标准串口，最常用的一种串行通讯接口。它是在1970年由美国电子工业协会（EIA）联合贝尔系统、调制解调器厂家及计算机终端生产厂家共同制定的用于串行通讯的标准。它的全名是“ 数据终端设备（DTE）和数据通讯设备（DCE）之间串行二进制数据交换接口技术标准”。传统的RS-232-C接口标准有22根线，采用标准25芯D型插头座（DB25），后来简化为D15再使用简化为9芯D型插座（DB9），现在应用中15芯、25芯插头座已很少采用。

**DB25 DB15 DB9插座**

![img](./img/wKgP3GIpyieAchEvAAUpZdJdkgA282.png)

**DB25 DB15 DB9线序图**

![img](./img/wKgP3GIpyieANba3AAGDiRJq6tI96.jpeg)

  RS-232采取不平衡传输方式，即所谓单端通讯。由于其发送电平与接收电平的差仅为2V至3V左右，所以其共模抑制能力差，再加上双绞线上的分布电容，其传送距离最大为约15米，最高速率为20kb/s。RS-232是为点对点（即只用一对收、发设备）通讯而设计的，其驱动器负载为3～7kΩ。所以RS-232适合本地设备之间的通信。

**RS-232 常见通信连接**

![img](./img/wKgP3GIpyieAC1GGAAhqNgtS6UE697.png)

**2、RS-422**

  标准全称是“平衡电压数字接口电路的电气特性”，它定义了接口电路的特性。典型的RS-422是四线接口。实际上还有一根信号地线，共5根线。其DB9连接器引脚定义。由于接收器采用高输入阻抗和发送驱动器比RS232更强的驱动能力，故允许在相同传输线上连接多个接收节点，最多可接10个节点。即一个主设备（Master），其余为从设备（Slave），从设备之间不能通信，所以RS-422支持点对多的双向通信。接收器输入阻抗为4k，故发端最大负载能力是10×4k+100Ω（终接电阻）。RS-422四线接口由于采用单独的发送和接收通道，因此不必控制数据方向，各装置之间任何必须的信号交换均可以按软件方式（XON/XOFF握手）或硬件方式（一对单独的双绞线）实现。

  RS-422的最大传输距离为1219米，最大传输速率为10Mb/s。其平衡双绞线的长度与传输速率成反比，在100kb/s速率以下，才可能达到最大传输距离。只有在很短的距离下才能获得最高速率传输。一般100米长的双绞线上所能获得的最大传输速率仅为1Mb/s。

**RS-422 常见通信连接**

![img](./img/wKgP3GIpyieALmNZAAGvfP-Eu_A623.png)

**3、RS-485**

  是从RS-422基础上发展而来的，所以RS-485许多电气规定与RS-422相仿。如都采用平衡传输方式、都需要在传输线上接终接电阻等。RS-485可以采用二线与四线方式，二线制可实现真正的多点双向通信，而采用四线连接时，与RS-422一样只能实现点对多的通信，即只能有一个主（Master）设备，其余为从设备，但它比RS-422有改进，无论四线还是二线连接方式总线上可多接到32个设备。

  RS-485与RS-422的不同还在于其共模输出电压是不同的，RS-485是-7V至+12V之间，而RS-422在-7V至+7V之间，RS-485接收器最小输入阻抗为12kΩ、RS-422是4kΩ；由于RS-485满足所有RS-422的规范，所以RS-485的驱动器可以在RS-422网络中应用。

  RS-485与RS-422一样，其最大传输距离约为1219米，最大传输速率为10Mb/s。平衡双绞线的长度与传输速率成反比，在100kb/s速率以下，才可能使用规定最长的电缆长度。只有在很短的距离下才能获得最高速率传输。一般100米长双绞线最大传输速率仅为1Mb/s。

**RS-485 常见通信连接**

![img](./img/wKgP3GIpyieAK8caAAHQz6ACvq4753.png)

**RS-485 延长TTL信号传输**

![img](./img/wKgP3GIpyieAfIKgAAHFRbCyDok770.png)

### 1.4 RS-232针脚

**RS-232 公母口针脚**

![img](./img/wKgP3GIpyieAdPfdAACatPiV-cU36.jpeg)

**RS-232 针脚简述**

| 针号 |    引脚功能    | 缩写 |
| :--: | :------------: | :--: |
|  1   |  数据载波检测  | DCD  |
|  2   |    接收数据    | RXD  |
|  3   |    发送数据    | TXD  |
|  4   |  数据终端准备  | DTR  |
|  5   |     信号地     | GND  |
|  6   | 数据设备准备好 | DSR  |
|  7   |    请求发送    | RTS  |
|  8   |    清除发送    | CTS  |
|  9   |    振铃指示    | DELL |

| 引脚编号 | 信号名称 | 方向 (从DTE出发) | 全称                            | 说明与用途                                                   |
| -------- | -------- | ---------------- | ------------------------------- | ------------------------------------------------------------ |
| 1        | DCD      | 输入             | Data Carrier Detect载波检测     | 由Modem（数据通信设备）发出，表示已检测到载波（即已连接到远程Modem）。 |
| **2**    | **RXD**  | **输入**         | **Receive Data****接收数据**    | **核心数据线，用于接收来自对方设备的数据。**                 |
| **3**    | **TXD**  | **输出**         | **Transmit Data****发送数据**   | **核心数据线，用于发送数据到对方设备。**                     |
| 4        | DTR      | 输出             | Data Terminal Ready数据终端就绪 | 由计算机（DTE）发出，表示本机已准备就绪。                    |
| **5**    | **GND**  | **-**            | **Signal Ground****信号地**     | **公共参考地线，所有信号的电压基准。必须连接。**             |
| 6        | DSR      | 输入             | Data Set Ready数据设备就绪      | 由Modem（DCE）发出，表示本设备已通电并准备就绪。             |
| 7        | RTS      | 输出             | Request To Send请求发送         | 硬件流控制信号。DTE发出，表示它想要发送数据。                |
| 8        | CTS      | 输入             | Clear To Send清除发送           | 硬件流控制信号。DCE发出，回应RTS，表示DCE已准备好接收数据。  |
| 9        | RI       | 输入             | Ring Indicator振铃指示          | 由Modem发出，表示电话线正在振铃（有来电）。                  |

**RS-232 针脚功能详述**

（1）数据装置准备好（DSR），有效状态（ON）表示数据通信设备处于可以使用状态。

（2）数据终端准备好（DTR），有效状态（ON）表示数据终端设备处于可以使用状态。这两个设备状态信号有效，只表示设备本身可用，并不说明通信链路可以开始进行通信了，能否开始进行通信要由下面的一些控制信号决定。

（3）请求发送（RTS），用来表示数据终端设备（DTE）请求数据通信设备（DCE）发送数据。

（4）允许发送（CTS），用来表示数据通信设备（DCE）已经准备好了数据，可以向数据终端设备（DTE）发送数据，是对请求发送信号RTS的响应。请求发送（RTS）和允许发送（CTS）用于半双工的通信系统中，在全双工的系统中，不需要使用请求发送（RTS）和允许发送（CTS）信号，直接将其置为ON即可。

（5）数据载波检出（DCD），用于表示数据通信设备（DCE）已接通通信链路，告知数据终端设备（DTE）准备接收数据。

（6）振铃指示（RI），当数据通信设备收到交换台送来的振铃呼叫信号时，使该信号有效（ON），通知终端，已被呼叫。

（7）发送数据（TXD），数据终端设备（DTE）通过该信号线将串行数据发送到数据通信设备（DCE）。

（8）接收信号（RXD），数据终端设备（DTE）通过该信号线接收从数据通信设备（DCE）发来的串行数据。

（9）地线（SG、PG），分别表示信号地和保护地信号线。

### 1.5 帧格式

  通信双方传输字符的格式要按规定写。在异步传输方式中，字符的位数、停止位的位数（1位，1.5位，2位），是否使用奇、偶校验位等命令，要通过对通信控制器写操作完成设置。

![img](./img/wKgP3GIpyieAEKMUAACX3K6wsBU52.jpeg)

**示例**

  串口3工作在9600bps，连续发送0xA9（1010 1001）（LSB：最低有效位优先传输），使用usb逻辑分析仪等到下图分析结果。

![img](./img/wKgP3GIpyieAKcA3AAF4kR6esyI92.jpeg)

### 1.6 奇偶检验

  奇偶校验(Parity Check)是一种校验代码传输正确性的方法。根据被传输的一组二进制代码的数位中“1”的个数是奇数或偶数来进行校验。
  采用奇数的称为奇校验，反之，称为偶校验。
  采用何种校验是事先规定好的。通常专门设置一个奇偶校验位，用它使这组代码中“1”的个数为奇数或偶数。
  若用奇校验，则当接收端收到这组代码时，校验“1”的个数是否为奇数，从而确定传输代码的正确性。

**奇校验**

![img](./img/wKgP3GIpyieAAR8EAACmlA9MGRA35.jpeg)

**偶校验**

![img](./img/wKgP3GIpyieAYfXDAACqAh5PRz806.jpeg)

  奇偶校验位是最简单的错误检测码。如果传输过程中包括校验位在内的奇数个数据位发生改变，那么奇偶校验位将出错表示传输过程有错误发生。

  因此，奇偶校验位是一种错误检测码，但是由于没有办法确定哪一位出错，所以它不能进行错误校正。发生错误时必须扔掉全部的数据，然后从头开始传输数据。

  在噪声很多的媒介上成功传输数据可能要花费很长的时间，甚至根本无法实现。但是奇偶校验位也有它的优点，它是使用一位数据能够达到的最好的校验码，并且它仅仅需要一些异或门就能够生成。奇偶校验被广泛应用。

### 1.7 **流控**

  数据在两个串口之间进行通讯的时候常常会出现丢失数据的现象，比如两台计算机或者是一台计算机和一个单片机之间进行通讯，当接收端的数据缓冲区已经满了，这个时候如果还有数据发送过来，因为接收端没有时间进行处理，那这样的数据就有可能会丢失。在工业现场或者其他领域，经常会遇到这种问题，本质原因是**速度不匹配、处理能力不匹配**。比如单片机的主频只有20M或30M，ARM的处理能力可能是200M，PC机的处理能力是几个G，这种处理能力的不匹配造成了传输的时候数据容易丢失。

  硬件流控就是来解决这个速度匹配的问题。**它的基本含义非常简单，当接收端接收到的数据处理不过来时，就向发送端发送不再接收的信号**，发送端接收到这个信号之后就会停止发送，直到收到可以继续发送的信号再继续发送。因此流控本身是可以控制数据传输的进度，进而防止数据丢失。

  一般常用的流控方式有两种：**硬件流控和软件流控**。

**硬件流控**

  如果使能了硬件流控，在三线串口通信模式增加两根控制线，一根叫 CTS(Clear To Send 为输入信号，一根叫 RTS(Require To Send 为输出信号)。一个是接收控制，一个是发送控制。

  从硬件连接原理图中我们可以看到，如果从 USART 1 向 USART 2 发送的话，USART 1 的 TX 和 USART 2 的 RX 相连，USART 1 的 CTS 和 USART 2 的 RTS 相连，数据的方向是从 TX 到 RX，从串口1到串口2，流控是从 RTS 到 CTS 也就是从串口2到串口1。

- RTS(Require To Send，发送请求)为输出信号，用于指示本设备准备好可接收数据，低电平有效，低电平说明本设备可以接收数据。

- CTS(Clear To Send，发送允许)为输入信号，用于判断是否可以向对方发送数据，低电平有效，低电平说明本设备可以向对方发送数据。


**软件流控**

  由于电缆线的限制，我们在普通的控制通讯中一般不用硬件流控制，而用软件流控制。一般通过xon/xoff来实现软件流控制。常用方法是：当接收端的输入缓冲区内数据量超过设定的高位时，就向数据发送端发出xoff字符（十进制的19或十六进制的0x13或control-s，设备编程说明书应该有详细阐述），发送端收到 xoff字符后就立即停止发送数据；当接收端的输入缓冲区内数据量低于设定的低位时，就向数据发送端发出xon字符（十进制的17或十六进制的0x11或control-q），发送端收到xon字符后就立即开始发送数据。一般可以从设备配套源程序中找到发送的是什么字符。

  应该注意，若传输的是二进制数据，标志字符也有可能在数据流中出现而引起误操作，这是软件流控制的缺陷，而硬件流控制不会有这个问题。

例如linux编程：

```
options.c_cc[VSTOP] = 0x22;	//要求对端停止发送数据
options.c_cc[VSTART]= 0x33;	//告诉对端可以发送数据
```



## **USART**

### **USART串口通信简介**

**USART（Universal Synchronous/Asynchronous Receiver/Transmitter）即通用同步/** **异步收发器** **，是一个全双工的串行通信接口** **，**广泛应用于嵌入式系统、工业控制、物联网设备等领域。

异步模式（UART)：无需时钟信号，通过起始位、数据位、校验位和停止位实现数据同步**（更普及）**。

同步模式（USART)：需要时钟信号同步，适用于高速数据传输



### **二、硬件连接**

USART / UART通讯（后面简称串口通讯）可以用一根线(TXD)发送数据的同时用另一根线接收(RXD)。

![img](./img/56765.png)



### **三、USART串口的电平标准**

根据通讯使用的电平标准不同，串口通讯可分为TTL标准及RS-232标准：

| 电平标准 | 逻辑 1       | 逻辑 0     | 特点与应用                                                   |
| -------- | ------------ | ---------- | ------------------------------------------------------------ |
| TTL      | +3.3V 或 +5V | 0V         | 最常见于微控制器领域：STM32、ESP32、Arduino等MCU的USART引脚直接输出就是TTL电平。 |
| RS-232   | -3V ~ -15V   | +3V ~ +15V | 1. 传统PC串口（DB9）的标准<br />2. 使用负逻辑和高电压<br />3. 抗干扰能力强，传输距离远（**可达15米**）<br />4. 需电平转换：MCU必须通过MAX232等芯片转换才能与PC串口通信。 |

 

![img](./img/56769.png)

 

![img](./img/56770.png)

> USB 差分信号
>
> TTL - USB
> IC(Integrated Circuit) / Chip: CH340、PL2102

### **四、UART串口协议数据帧格式**

异步通信没有时钟线，**所以双方必须事先约定好完全相同的通信参数，数据才能被正确解析**。这些参数构成了一个“数据帧”。

![img](./img/56771.png)

**一个完整的数据帧由以下几部分组成：**

| 组成部分 | 说明                                                         |
| -------- | ------------------------------------------------------------ |
| 空闲位   | 线路在未传输数据时保持在高电平（逻辑1）状态。                |
| 起始位   | 1个位时间的低电平（逻辑0）。这是一个“唤醒”信号，告诉接收方“数据要来了！”。它标志着数据帧的开始。 |
| 数据位   | 紧接着起始位之后，是要传输的实际数据。通常是5-9位（最常用的是8位）。**数据是低位（LSB）在前，高位（MSB）在后。** |
| 校验位   | 可选的1位，用于简单的错误检测：<br />1.偶校验：数据位+校验位中“1”的个数为偶数。<br />2.奇校验：数据位+校验位中“1”的个数为奇数。<br />3.无校验：最常见的设置。 |
| 停止位   | 1个、1.5个或2个位时间的高电平（逻辑1）。它标志着数据帧的结束，并确保下一帧的起始位有一个从高到低的下降沿。 |

 

### **五、USART串口常见的波特率**

| 波特率 (bps) | 常见应用场景与说明                                           |
| ------------ | ------------------------------------------------------------ |
| **9600**     | **最常用、最基础的波特率**。稳定性好，抗干扰能力强，几乎被所有硬件支持。是调试和简单通信的首选。 |
| 19200        | 比较常见，是9600的两倍。                                     |
| 38400        | 常见，是19200的两倍。                                        |
| 57600        | 常见，用于需要较高速度的通信。                               |
| **115200**   | **高速通信中最常用的波特率**。是目前单片机与电脑通信调试的“事实标准”，速度快且稳定。 |
| 921600       | 非常高速度，对硬件和时钟精度要求高。                         |



### **六、STM32F103x USART外设**

**1. STM32F103x串口数量**

在《STM32F103xx数据手册中文版.pdf》第 5 页，外设资源数量表中，可以看到，STM32F103Cx系列控制器有3个USART。

![img](./img/56772.png)

 

**2. USART外设控制的引脚**

在STM32中，一个物理引脚除了作为通用的输入/输出（GPIO）之外，还可以被配置为内部外设（如USART、SPI、I2C等）的连接引脚，这被称为**复用功能**。

你可以在《STM32F103xx数据手册中文版.pdf》第 17 页表格中，查询STM32芯片所有引脚的功能：

![img](./img/56773.png)

**从表中可以看到 PA9 引脚可以配置为多种复用功能之一，其中可以复用为 USART1_TX 引脚（即USART1外设的TXD引脚）。**

**3. USART 框图**

![img](./img/56774.png)

**硬件控制流引脚：**

💡

RTS：Request To Send → 请求发送，当前设备（ESP32） 向对方设备发送的信号；

CTS：Clear To Send → 允许发送，对方设备向当前设备（ESP32） 发送的信号。

✅ 场景 1：STM32 作为「接收方」（收数据）

当 STM32 的 UART 接收缓冲区快满时 → STM32 主动拉低「RTS 引脚」→ 通知对方设备：我缓存满了，暂停发送数据；当 STM32 处理完缓存数据、缓冲区空闲后 → STM32 拉高「RTS 引脚」→ 通知对方设备： 我准备好了，你可以继续发数据。

✅ 场景 2：STM32 作为「发送方」（发数据）

STM32 想要发送数据前，会先检测「CTS 引脚」电平 → 若为高电平（对方设备允许）→ 正常发送；若为低电平（对方设备忙）→  暂停发送，直到 CTS 引脚拉高。

 

**4. USART 中断类型**

你可以通过代码设置在以下的场景，触发USART的中断：

| **中断类型**       | **描述**                                                     | **使能控制位** **(在USART_CR1/CR3寄存器中)** |
| ------------------ | ------------------------------------------------------------ | -------------------------------------------- |
| 发送数据寄存器空   | 发送寄存器为空，可以写入下一个数据                           | TXEIE (CR1)                                  |
| 发送完成           | 一帧数据已完全发送完毕                                       | TCIE (CR1)                                   |
| 接收数据寄存器非空 | 已接收到数据，可读取                                         | RXNEIE(CR1)                                  |
| 检测到空闲总线     | RX线检测到空闲状态，一个数据包发送完成<br />可以用于不定长的接收 | IDLEIE(CR1)                                  |
| 奇偶校验错误       | 接收数据的奇偶校验失败                                       | PEIE(CR1)                                    |
| 溢出错误           | 数据未被读取，新数据已到，导致丢失                           | PEIE(CR1)                                    |
| 帧错误             | 停止位检测失败                                               | PEIE(CR1) 控制                               |
| 噪声错误           | 在帧中检测到噪声                                             | PEIE(CR1)                                    |
| LIN 断开符检测     | 检测到LIN总线要求的断开符                                    | LBDIE (CR2)                                  |
| CTS 标志中断       | CTS引脚状态变化                                              | CTSIE(CR3)                                   |

 

**5. USART 中断标志**

每一个中断类型都对应一个状态标志位，这些标志位主要存在于 USART_SR 状态寄存器中。当中断事件发生时，相应的标志位会被硬件自动置 1。

| **中断标志** | **标志位寄存器** | **标志位描述**     | **触发条件**                          |
| ------------ | ---------------- | ------------------ | ------------------------------------- |
| **TXE**      | USART_SR         | 发送数据寄存器空   | 发送数据寄存器(TDR)为空               |
| **TC**       | USART_SR         | 发送完成           | 一帧数据（含停止位）发送完成，且TXE=1 |
| **RXNE**     | USART_SR         | 接收数据寄存器非空 | 接收数据寄存器(RDR)已存入新数据       |
| **IDLE**     | USART_SR         | 检测到空闲总线     | RX线保持空闲（高电平）状态            |
| **ORE**      | USART_SR         | 溢出错误           | 数据未读（RXNE=1）时，又收到新数据    |
| **FE**       | USART_SR         | 帧错误             | 在停止位位置未检测到预期的'1'         |
| **NE**       | USART_SR         | 噪声错误           | 在接收的帧中检测到噪声                |
| **PE**       | USART_SR         | 奇偶校验错误       | 校验位与数据不匹配（校验使能时）      |
| **LBD**      | USART_SR         | LIN 断开符检测     | 检测到LIN断开符                       |
| **CTS**      | USART_SR         | CTS 状态变化       | CTS引脚输入状态发生变化               |

 

## 2、串口硬件设计

**原理图**

![img](./img/wKgP3GIpyieAEkRiAAHePs3u4sM010.png)

**实物图**

![img](./img/wKgP3GIpyieAKuf0AAZOi0jdZQE250.png)

*CH340芯片描述*

  CH340是一个USB总线的转接芯片，实现USB转串口或者USB转打印口。在串口方式下，CH340提供常用的MODEM联络信号，用于为计算机扩展异步串口。

![img](./img/wKgP3GIpyieARILcAAApqm_F0vI788.gif)

## 3 串口初始化

### 3.1 STM32CubeMX

#### **STM32CubeMX配置**

![img](./img/56776.png)

**Basic Parameters（基础参数）**

1. Baud Rate：115200 Bits/s

- 功能：串口通信波特率，每秒传输二进制比特数量；收发双方波特率必须完全一致，否则乱码。

- 当前配置：115200bps。常见标准值：9600、19200、115200。

1. Word Length：8 Bits (including Parity)

- 功能：一帧数据有效位长度，**包含奇偶校验位**。

- 当前：8位。

- 无校验(Parity=None)：实际数据 = 8bit

- 如果开启奇偶校验，则数据位7bit + 校验位1bit，合计8bit

1. Parity：None

- 功能：奇偶校验位，用于简单检错。

- None：不使用校验；可选：Even（偶校验）、Odd（奇校验）。

1. Stop Bits：1

- 功能：停止位，标志一帧数据结束。可选 0.5 / 1 / 1.5 / 2 bits；绝大多数场景使用1位停止位。

**Advanced Parameters（高级参数）**

1. Data Direction：Receive and Transmit

- 功能：串口收发方向选择

- Receive and Transmit：收发双向（TX、RX都启用）其他选项：仅接收、仅发送。

1. 过采样（Over Sampling）

- 可选：**16 Samples / 8 Samples**

- 功能：硬件采样外部串口电平的频率

- **16 Samples（默认）**：波特率时钟 = 16 × 波特率每1bit周期内采样16次，容错性强、抗干扰更好；低速串口首选。

- **8 Samples**：波特率时钟 = 8 × 波特率相同时钟下可以支持更高波特率；缺点：采样点变少，噪声更容易导致接收错误。

**注意：如果使用USART在中断函数处理接收数据，必须开启中断。**

![img](./img/56777.png)

 

![img](./img/56778.png)

 **3. 分析生成的代码**

![img](./img/56779.png )

**MCU Specific Package** **[spəˈsɪfɪk ˈpækɪdʒ] ，为单片机的具体方案。**

MSP是指和MCU相关的初始化。

初始化一个串口，首先要设置和 MCU 无关的东西，例如波特率，奇偶校验，停止位等，这些参数设置和 MCU 没有任何关系，可以使用 STM32F1，也可以是 STM32F2/F3/F4/F7上的串口。而一个串口设备它需要一个 MCU 来承载，例如用 STM32F4 来做承载，PA9 做为发送，PA10 做为接收，MSP 就是要初始化 STM32F4 的 PA9,PA10，配置这两个引脚。所以 HAL驱动方式的初始化流程就是：HAL_USART_Init()—>HAL_USART_MspInit() ，先初始化与 MCU无关的串口协议，再初始化与 MCU 相关的串口引脚。在 STM32 的 HAL 驱动中HAL_PPP_MspInit()作为回调，被 HAL_PPP_Init()函数所调用。当我们需要移植程序到 STM32F1平台的时候，我们只需要修改 HAL_PPP_MspInit 函数内容而不需要修改 HAL_PPP_Init 入口参数内容。

在HAL库中，几乎每初始化一个外设就需要设置该外设与单片机之间的联系，比如IO口，是否复用等等，可见，HAL库相对于标准库多了MSP函数之后，移植性非常强，但与此同时却增加了代码量和代码的嵌套层级。可以说各有利弊。

同样，MSP函数又可以配合句柄，达到非常强的移植性：

```C
void HAL_UART_MspInit(UART_HandleTypeDef *huart);
```

 入口参数仅仅需要一个串口句柄，这样有能看出句柄的方便。

 

**4. 中断服务程序**

STM32CubeMX生成的中断服务程序是统一放到文件 stm32f1xx_it.c 的，USART1对应的中断服务程序为 USART1_IRQHandler() ：

![img](./img/56780.png)

![img](./img/56781.png)

 

1.在“categories”类别中就选择USART1。

![img](./img/wKgP3GIpyieALPn8AAAYpH0wHvM053.png)

2.模式选择Asynchronous（异步通信）；硬件流控制取消。

![img](./img/wKgP3GIpyieANjyVAAANbEwdCYE570.png)

3.USART串口参数1配置，如下图默认即可，波特率为115200、8位数据位、无校验位、1位停止位。16倍过采样，提高采样的准确率，降低了速度容差与时钟容差。使用8倍过采样，就会降低采样的准确率，但是会提高波特率的上限。

![img](./img/wKgP3GIpyieAN47EAAA64NdYW4Y250.png)

4.芯片对应的PA9，PA10引脚，自动配置为USART1_RX模式和USART1_TX模式。

![img](./img/wKgP3GIpyieATfGuAAAeMlbtnm8707.png)

5.“NVIC Settings”中使能“USART1 global interrupt”,并设置好优先级。

![img](./img/wKgP3GIpyieAG7wGAAAeNTWT1zU417.png)

### 3.2 代码分析

**1、USART初始化**

```C
UART_HandleTypeDef huart1;

/* HAL库使用的串口接收缓冲 */
uint8_t g_hal_uart_rx_data=0;

/* USART1 init function */

void MX_USART1_UART_Init(void)
{
  huart1.Instance = USART1;
  huart1.Init.BaudRate = 115200;
  huart1.Init.WordLength = UART_WORDLENGTH_8B;
  huart1.Init.StopBits = UART_STOPBITS_1;
  huart1.Init.Parity = UART_PARITY_NONE;
  huart1.Init.Mode = UART_MODE_TX_RX;
  huart1.Init.HwFlowCtl = UART_HWCONTROL_NONE;
  huart1.Init.OverSampling = UART_OVERSAMPLING_16;
  if (HAL_UART_Init(&huart1) != HAL_OK)
  {
    Error_Handler();
  }
  
  	/*自行添加：该函数会开启接收中断：标志位UART_IT_RXNE，并且设置接收缓冲以及接收缓冲接收最大数据量 */
	HAL_UART_Receive_IT(&huart1, &g_hal_uart_rx_data, 1);
}
```

**2、USART低级初始化**

```C
void HAL_UART_MspInit(UART_HandleTypeDef* uartHandle)
{
  GPIO_InitTypeDef GPIO_InitStruct = {0};
  if(uartHandle->Instance==USART1)
  {
    /* USART1 clock enable */
    __HAL_RCC_USART1_CLK_ENABLE();
  
    __HAL_RCC_GPIOA_CLK_ENABLE();
    /**USART1 GPIO Configuration    
    PA9     ------> USART1_TX
    PA10    ------> USART1_RX 
    */
    GPIO_InitStruct.Pin = GPIO_PIN_9|GPIO_PIN_10;
    GPIO_InitStruct.Mode = GPIO_MODE_AF_PP;
    GPIO_InitStruct.Pull = GPIO_PULLUP;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_VERY_HIGH;
    GPIO_InitStruct.Alternate = GPIO_AF7_USART1;
    HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);

    /* USART1 interrupt Init */
    HAL_NVIC_SetPriority(USART1_IRQn, 3, 0);
    HAL_NVIC_EnableIRQ(USART1_IRQn);
  }
}
```

**3、USART接收完成回调函数**

```C
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    /* 判断是由哪个串口触发的中断 */
	if(huart->Instance == USART1)					
	{
        /* 接收到数据马上使用串口1发送出去*/
		HAL_UART_Transmit(&huart1,&g_hal_uart_rx_data,1,100);
  
        /* 当进入HAL_UART_RxCpltCallback函数，默认关闭串口中断配置，需重新使能 */
		HAL_UART_Receive_IT(&huart1,&g_hal_uart_rx_data,1);	
	}
}
```

**4、主函数**

```C
int main(void)
{
  HAL_Init();

  SystemClock_Config();

  MX_GPIO_Init();
  MX_USART1_UART_Init();

  while (1)
  {
	/* 串口1发送字符串"hello teacher.wen\r\n"，长度19字节，超时时间为1秒*/
	HAL_UART_Transmit(&huart1,"hello teacher.wen\r\n",19,1000);

	/* 延时2秒 */
	HAL_Delay(2000);
  }
}
```

### 3.3 操作演示

  当上述程序成功烧录到粤嵌STM32F429开发板后，串口调试助手的波特率选中115200，观察到[接收区]中每2秒显示“hello teacher.wen”，另外在[发送区]输入要发送的内容如“stm32”，接着点击[发送]按钮，开发板则回显串口调试助手发送的内容，演示效果如下。

![img](./img/wKgP3GIpyieAI0cYAADqexZIupA230.gif)



### **添加用户代码**

**1. 添加代码文件**

![img](./img/56782.png)

 

![img](./img/56783.png)

 

**2. 实验1（轮询接收）**

使用轮询的方式收发。轮询的方式就是在循环中一直检测串口，是否接收到数据，如果有就读取。

**2.1 debug.c**

```C
#include "debug.h"
#include "usart.h" // STM32CubeMx生成
 
void debug_test1(void)
{
    uint8_t buff[10];
    while (1)
    {
        // 指定串口1接收10字节数据，HAL_MAX_DELAY阻塞永久等待，
        // 直到接收到数据。如果接收到数据，则返回HAL_OK    。
        // 如果你们不希望阻塞，可以指定一个超时时间，如1000 (1000ms)。
        if (HAL_UART_Receive(&huart1, buff, 10, HAL_MAX_DELAY) == HAL_OK)
        {
            // 把收到的数据原封不动的发出去, HAL_MAX_DELAY阻塞永久等待
            // 你也可以单独调用该函数主动发送你需要的数据
            HAL_UART_Transmit(&huart1, buff, 10, HAL_MAX_DELAY);
        }
    }
}
 
```

**2.2 debug.h**

```C
#ifndef DEBUG_H
#define DEBUG_H
 
#include "main.h"
void debug_test1(void);
 
#endif // DEBUG_H
 
```

 

**2.3 app_main.c**

```C
#include "app_main.h"
#include "main.h"
#include "tim.h" // 包含STM32CubeMX生成的TIM头文件
#include "debug.h" // 包含头文件，声明函数
......省略代码显示
// 替代main函数中的while循环
void app_main(void)
{
    // HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_1);         // 启动定时器1的PWM输出
    // __HAL_TIM_SetCompare(&htim1, TIM_CHANNEL_1, 300); // 设置定时器1，CCR1为300，这时PWM占空比30%
    debug_test1();  // 测试串口1轮询接收
    while (1)
    {
    }
}
 
```

 

**3. 串口调试助手**

![img](./img/56784.png)

硬件接线无问题后，打开PortHelper.exe，使用里面的串口调试助手功能验证代码

**3.** **1 查看端口号**

开始 -> 设备管理器 -> 端口，查看到我的端口是 COM3

![img](./img/56785.png)

 或者

![image-20260820192633470](./img/image-20260820192633470.png)

如果硬件接线后，每发现端口，可能是没安装驱动，安装老师提供的驱动即可。

![img](./img/56786.png)

若在win11中串口打不开，CH340驱动要选择老版本的驱动。

![img](./img/56787.png)

 

**3.** **2 PortHelper.exe中打开串口**

![img](./img/56788.png)

 

**3.** **3 验证**

![img](./img/56789.png)

**轮询方式的特点**：

- 轮询模式使用起来最简单

- 但是会占用大量的CPU时间，在等待接收和等待发送完毕时，CPU不能去做别的运算，只能在这里空等，运行的效率很低。

- 要等待接收完指定长度的数据后，才会返回。

 

**4. 实验2（中断接收定长数据）**

前面我们分析代码可知，当开启接收指定长度数据时，收到指定长度数据时，运行void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)这个回调函数。该函数需要我们定义，实现我们需要的功能。

**4.1 debug.c**

```C
#include "debug.h"
#include "usart.h" // STM32CubeMx生成
 
uint8_t buff[10];
// 这个函数被HAL_UART_RxCpltCallback()调用
void debug_UART_test2_Callback(void)
{
    // 把收到的数据原封不动的发出去
    HAL_UART_Transmit(&huart1, buff, 10, HAL_MAX_DELAY);
    
    // 重新用中断的方式接收10个字节的数据，继续接收
    HAL_UART_Receive_IT(&huart1, buff, 10);
}
 
// 测试串口1中断接收定长数据
void debug_test2(void)
{
    // 用中断的方式接收10个字节的数据
    HAL_UART_Receive_IT(&huart1, buff, 10);
}
 
```

 

**4.2 debug.h**

```C
#ifndef DEBUG_H
#define DEBUG_H
 
#include "main.h"
void debug_UART_test2_Callback(void);
void debug_test2(void);
#endif // DEBUG_H
 
```

 

**4.3 ISR_callback.c**

中断被调用的用户代码统一放这里，当然你也可以自己规定放在某个方便管理的地方。

```C
#include "ISR_callback.h"
#include "app_main.h"
#include "debug.h"
 
// 定义自己的EXTI中断调用的函数
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
{
        if (GPIO_Pin == KEY1_Pin)   // 判断是否是KEY1引脚触发的中断
        {
            delay_ms(15);  // 消抖按下瞬间的电压
            if (HAL_GPIO_ReadPin(KEY1_GPIO_Port, KEY1_Pin) == GPIO_PIN_RESET)
            {
                    HAL_GPIO_TogglePin(LED_GPIO_Port, LED_Pin); // 翻转LED
                    delay_ms(15);// 消抖松开时瞬间的电压
                    while(HAL_GPIO_ReadPin(KEY1_GPIO_Port, KEY1_Pin) == GPIO_PIN_RESET); // 等待高电平按键松开 
            }
        }
}
 
// 定义自己的UART接收完成调用的函数
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    if(huart->Instance == USART1)    // 串口1接收定长数据触发中断
    {
        debug_UART_test2_Callback();    
    }
}

```

 

**4.4 app_main.c**

```C
#include "app_main.h"
#include "main.h"
#include "tim.h" // 包含STM32CubeMX生成的TIM头文件
#include "debug.h"
......省略代码显示
// 替代main函数中的while循环
void app_main(void)
{
    // HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_1);         // 启动定时器1的PWM输出
    // __HAL_TIM_SetCompare(&htim1, TIM_CHANNEL_1, 300); // 设置定时器1，CCR1为300，这时PWM占空比30%
    debug_test2(); // 测试串口1中断接收
    while (1)      // 你可以在这里处理其它事情的代码
    {
        HAL_GPIO_TogglePin(LED_GPIO_Port, LED_Pin);
        HAL_Delay(1000);
    }
}
 
```

 

**4.5 现象**

![img](./img/56790.png)

**中断定长接收数据方式的特点：**

- 不占用大量的CPU时间，只有在接收到定长数据时，才会运行中断服务函数，在这期间可以运行其它的代码。

- 只能接收定长的数据，不灵活。

 

**5. 实验3（中断接收变长数据）**

前面我们分析代码可知，当开启接收空闲中断，接收空闲（不管数据多长，只要数据不再传输，则接收空闲）时，运行`void HAL_UARTEx_RxEventCallback(UART_HandleTypeDef *huart, uint16_t Size)`这个回调函数。该函数需要我们定义，实现我们需要的功能。

**5.1 debug.c**

```C
#include "debug.h"
#include "usart.h" // STM32CubeMx生成
 
uint8_t buff[10];
 
// 这个函数被HAL_UARTEx_RxEventCallback()调用
void debug_UART_test3_Callback(uint16_t Size)
{
    // 把收到的数据原封不动的发出去
    HAL_UART_Transmit(&huart1, buff, Size, HAL_MAX_DELAY);
    
    // 重新用中断的方式接收到10个字符或者碰到空闲帧, 则接收结束
    HAL_UARTEx_ReceiveToIdle_IT(&huart1, buff, 10);
}
 
// 测试串口1中断接收变长数据
void debug_test3(void)
{
    // 当接收到10个字符或者碰到空闲帧, 则接收结束
    HAL_UARTEx_ReceiveToIdle_IT(&huart1, buff, 10);
}
 
```

**5.2 debug.h**

```C
#ifndef DEBUG_H
#define DEBUG_H
 
#include "main.h"
void debug_UART_test3_Callback(uint16_t Size);
void debug_test3(void);
#endif // DEBUG_H
 
```

**5.3 ISR_callback.c**

```C
#include "ISR_callback.h"
#include "app_main.h"
#include "debug.h"
 
// 定义自己的EXTI中断调用的函数
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
{
        if (GPIO_Pin == KEY1_Pin)   // 判断是否是KEY1引脚触发的中断
        {
            delay_ms(15);  // 消抖按下瞬间的电压
            if (HAL_GPIO_ReadPin(KEY1_GPIO_Port, KEY1_Pin) == GPIO_PIN_RESET)
            {
                    HAL_GPIO_TogglePin(LED_GPIO_Port, LED_Pin); // 翻转LED
                    delay_ms(15);// 消抖松开时瞬间的电压
                    while(HAL_GPIO_ReadPin(KEY1_GPIO_Port, KEY1_Pin) == GPIO_PIN_RESET); // 等待高电平按键松开 
            }
        }
}
 
// 定义自己的UART接收空闲调用的函数
void HAL_UARTEx_RxEventCallback(UART_HandleTypeDef *huart, uint16_t Size)
{
    if(huart->Instance == USART1) // // 串口1接收变长数据触发中断
    {
        debug_UART_test3_Callback(Size);
    }
}
 
```

**5.4 app_main.c**

```C
#include "app_main.h"
#include "main.h"
#include "tim.h" // 包含STM32CubeMX生成的TIM头文件
#include "debug.h"
......省略代码显示
// 替代main函数中的while循环
void app_main(void)
{
    // HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_1);         // 启动定时器1的PWM输出
    // __HAL_TIM_SetCompare(&htim1, TIM_CHANNEL_1, 300); // 设置定时器1，CCR1为300，这时PWM占空比30%
    debug_test3(); // 测试串口1中断接收
    while (1)      // 你可以在这里处理其它事情的代码
    {
        HAL_GPIO_TogglePin(LED_GPIO_Port, LED_Pin);
        HAL_Delay(1000);
    }
}
 
```

**5.5 现象**

![img](./img/56791.png)

 **6. 总结**

**6.1 串口轮询接收**

阻塞式工作，CPU 主动查询串口状态，接收期间无法执行其他任务。

实现简单无需配置中断，核心 API 为HAL_UART_Receive()，资源占用少。

适合简单调试、单次少量数据接收，不支持多任务和高实时性场景。

**6.2 串口中断定长接收**

非阻塞式工作，定长数据接收完成触发HAL_UART_RxCpltCallback()回调。

核心闭环为「开启中断→定长完成→回调重开中断」，核心 APIHAL_UART_Receive_IT()。

CPU 利用率高、实时性强，适合工业控制等固定长度数据通信场景。

**6.3 串口中断变长接收**

非阻塞式工作，以结束符 / 帧头帧尾为标志判断接收完成，逐字节中断接收。

实现灵活（支持变长数据），需在回调中做数据校验和结束判断，复杂度高于定长。

适合无固定数据长度的通信场景，需注意缓冲区溢出和帧同步错误问题。

**九、练习**

将uart1 中断接收变长数据功能添加到你的工程中。

参考：

```C
#include "debug.h"
#include "usart.h" // STM32CubeMx生成
 
uint8_t debug_buffer[DEBUG_RX_BUF_SIZE]; // debug接收缓冲  DEBUG_RX_BUF_SIZE=>128
uint16_t debug_rx_len;                    // debug接收长度
 
// 这个函数被HAL_UARTEx_RxEventCallback()调用
void debug_UART_Callback(uint16_t Size)
{
    debug_rx_len = Size;
    HAL_UARTEx_ReceiveToIdle_IT(&huart1, debug_buffer, sizeof(debug_buffer));// 继续开启接收数据
}
 
// 启动中断接收变长数据
void Debug_UART_Receive_Start(void)
{
    debug_rx_len = 0;
    memset(debug_buffer, 0, sizeof(debug_buffer));
    HAL_UARTEx_ReceiveToIdle_IT(&huart1, debug_buffer, sizeof(debug_buffer));
}
 
```

 

## 环形缓冲区





##  应用场景

**1.指纹锁**

**2.手机无线传输模块，如WiFi模块、蓝牙模块、GPRS模块、4G模块、GPS模块、LoRa模块等。**

GPRS模块

![img](./img/wKgP3GIpyieAeUuXAAsMnEMLGlY421.png)

WiFi模块

![img](./img/wKgP3GIpyieAWIZoAAJFhHq06v0762.png)

LoRa模块

![img](./img/wKgP3GIpyieAJTjgAAL9YSdM7y4800.png)

**3.超级终端**

![img](./img/wKgP3GIpyieABlnoAABKmvmRtYQ211.png)

**4.串口屏**

![img](./img/wKgP3GIpyieAIfwRAAQWveEhsSo771.png)



# 1 重定向printf

## 1.1 printf概述

  printf()函数是格式化输出函数, 一般用于向标准输出设备按规定格式输出信息。在编写程序时经常会用到此函数。函数的原型为：

```C
int printf(const char *format, ...);
```

  函数返回值为整型。若成功则返回输出的字符数，输出出错则返回负值。

## 1.2 构建printf

**1、权威指南**

**《Cortex M3与M4权威指南.pdf》章节18.1 P583**

> A common task for beginners is to generate a simple output message of “Hello world!” In C language, this is commonly handled with a “printf” statement. Under the hood, the message output can be redirected to different forms of communication interfaces. Typically this is known as re-targeting. For example, it is very common to retarget printf to a UART during embedded software development.

**《Cortex M3与M4权威指南.pdf》章节18.2.2 P584**

> In Keil MDK-ARM (or other ARM toolchains such as DS-5 Professional), the function that needs to be implemented to support printf is “fputc.”

```C
#include "stm32f4xx.h"
#include <stdio.h>

int fputc(int ch, FILE *f) {
	return (ITM_SendChar(ch));
}
```

**2、fputc重构**

（1）MDK勾选“MicroLIB”。

![img](./img/wKgP3GIpyjqAYZduAAB5lSET1qI781.png)

（2）在C文件中添加以下代码。

```C
#include "stm32f4xx.h"
#include <stdio.h>

int fputc(int ch,FILE *f)
{
	HAL_UART_Transmit(&huart1,(uint8_t *)&ch,1,0xffff);
	return ch;
}
```

- f103 不要MDK勾选“MicroLIB”

```C
#include <stdio.h>
struct __FILE {int handle;};
FILE __stdout;
FILE __stdin;
int fputc(int ch, FILE *stream) {
#if 0	
    // 对性能影响大
    uint8_t tx_byte = (uint8_t)ch;

    (void)stream;
    (void)HAL_UART_Transmit(&huart1, &tx_byte, 1U, HAL_MAX_DELAY);
    // HAL_UART_Transmit(&huart1, (uint8_t *)&ch, 1, 1000); 
#else
    while((USART1->SR & (1 << 6)) == 0); // 循环发送,直到发送完毕
    USART1->DR = (uint16_t)ch;
#endif
    return ch;
}
```

> printf的调用链路长，内部while

## 1.3 printf输出测试

（1）在main函数调用printf函数。

```C
int main(void)
{
  HAL_Init();

  SystemClock_Config();

  MX_GPIO_Init();
  MX_USART1_UART_Init();

  printf("This is printf test\r\n");

  while (1)
  {
  }
}
```

（2）在串口调试助手中成功观察到printf输出的信息。

![img](./img/wKgP3GIpyjqAJBN3AAA7arr5jr8168.png)



# **WIFI**

### **一、概述**

后面我们将实现终端节点的无线联网能力，大概流程如下。LoRa 通信部分我们已经完成，接下来我们将实现MQTT部分的通信。

![img](./img/56604.png)

### **二、WIFI 模块**

MQTT是一种基于 TCP/IP 协议栈的轻量级、低功耗的发布 / 订阅模式物联网通信协议。因此网关和巴法云实现MQTT协议通信，必须要有**联网**功能。为了更好实现联网功能，网关采用了 MCU + WIFI 模块方案，即网关搭载了一款 [WIFI 模块—— ESP-12E](https://docs.ai-thinker.com/esp8266/#_1-产品简介)。

![img](https://share.note.youdao.com/yws/public/resource/f17d7df84e9ce036d04d9c597fdc8125/xmlnote/WEBe365c08a0db44672b6f318ce28b4fcda/WEBRESOURCEda3e193c84504b3282258bd965d028c4/56605)

 

### **三、MCU和WIFI硬件连接**

STM32 主要使用 **USART** 和 WIFI 模块通信，间接实现联网功能，硬件连接如下：

![img](./img/56606.png)

  

### **四、WIFI 连网流程**

**1. USART参数**

![img](./img/56607.png)

 

**2.** **连网流程**

STM32 通过串口向 ESP-12E 发送一系列 AT 指令，控制 WiFi 模块连接本地路由器，获取互联网接入能力，核心指令流程：

```C
[23:31:49.490]发→◇+++□    #退出透传模式
[23:31:52.674]发→◇AT      #测试wifi是否正常工作，正常返回OK
[23:31:52.678]收←◆
OK
[23:32:08.107]发→◇ATE0    #关闭回响(发送的命令不返回)
[23:32:08.112]收←◆
OK
 
[23:32:13.738]发→◇AT+CWMODE=3  #混合wifi模式 (去连接wifi路由器)
[23:32:13.744]收←◆
OK
 
[23:32:15.834]发→◇AT+CWJAP="SSID","PASSWD"    #配置WiFi连接
[23:32:16.083]收←◆WIFI CONNECTED
 
[23:32:17.011]收←◆WIFI GOT IP        #WIFI连接成功
 
OK
 
[23:32:17.570]发→◇AT+CIPMODE=1       #开启透明传输模式
[23:32:17.576]收←◆
OK
 
[23:32:20.586]发→◇AT+CIPSTART="TCP","bemfa.com",9501  #连接巴法云服务器和端口
[23:32:20.675]收←◆CONNECT
 
OK
 
[23:32:22.347]发→◇AT+CIPSEND        #进入透传模式，后面发的都会无条件传输到服务器
[23:32:22.353]收←◆
OK
 
>
```

**(注意：每次发送 AT 指令给WIFI模块时，除了 "+++" 指令外，其它指令后必须\r\n结尾。）**

 

**3. 测试AT指令**

网关wifi模块AT指令测试

.rar

588.99KB

![img](./img/56610.png)

 

### **五、将连网流程写成代码**

**1. STM32CubeMX配置**

![img](./img/56611.png)

![img](./img/56612.png)

 

**2. 代码示例**

**2.1** **添加代码文件**

![img](./img/56613.png)

**2.2 esp.c**

```C
/*
 * ============================================================================
 * 文件名称：esp.c
 * 文件描述：ESP12-F Wi-Fi模块驱动实现文件（基础版）
 *
 * 功能描述：
 *   本文件实现了ESP12-F Wi-Fi模块的基础驱动功能，包括：
 *   1. 串口通信初始化和数据收发（USART2，HAL库）
 *   2. AT指令发送和响应解析
 *   3. AT指令集测试（esp_test1）
 *
 * 硬件说明：
 *   - ESP12-F连接在USART2（TX: PA2, RX: PA3）
 *   - 波特率：115200bps
 *   - AT指令集与ESP8266完全兼容
 *   - 使用HAL_UARTEx_ReceiveToIdle_IT实现空闲中断接收
 *   - 调试输出通过USART1（PA9/PA10）printf打印
 *
 * 作者：温老师
 * 日期：2026/05/09
 * 版本：V1.0（基础版）
 * ============================================================================
 */
 
#include "main.h"
#include "usart.h"
#include "delay.h"
#include <string.h>
#include <stdlib.h>
#include <stdio.h>
 
/* 包含头文件 */
#include "esp.h"
 
/* ==================== 全局变量定义 ==================== */
 
/**
 * @brief 发送缓冲区
 */
uint8_t g_esp_tx_buf[ESP_BUF_SIZE];
 
/**
 * @brief 接收缓冲区
 */
uint8_t g_esp_rx_buf[ESP_BUF_SIZE];
 
/**
 * @brief 接收数据计数
 */
uint32_t g_esp_rx_cnt = 0;
 
/**
 * @brief 接收完成标志
 */
uint32_t g_esp_rx_end = 0;
 
 
/* ==================== 公共函数实现 ==================== */
 
/**
 * @brief 初始化ESP模块
 *
 * 重新配置USART2波特率为115200bps（ESP12-F默认波特率），
 * 启动空闲中断接收，初始化状态机
 */
void esp_init(void)
{
    /* 重新配置USART2波特率为115200（CubeMX默认生成9600，ESP需要115200） */
    huart2.Init.BaudRate = 115200;
 
    HAL_UART_Init(&huart2);
 
    /* 清空缓冲区并启动接收 */
    esp_clear_rx_buf();
}
 
/**
 * @brief 串口2接收事件处理函数（由HAL回调函数调用）
 *
 * 将接收到的数据追加到接收缓冲区，并重新启动下一次接收
 *
 * @param size 本次实际接收到的字节数
 */
void esp_rx_event_handler(uint16_t size)
{
    /* 累加接收计数 */
    g_esp_rx_cnt += size;
    g_esp_rx_end = 1;
 
    /* 缓冲区未满时，继续从当前偏移处接收下一帧数据 */
    if (g_esp_rx_cnt < ESP_BUF_SIZE)
    {
        HAL_UARTEx_ReceiveToIdle_IT(&huart2,
                                    (uint8_t*)&g_esp_rx_buf[g_esp_rx_cnt],
                                    ESP_BUF_SIZE - g_esp_rx_cnt);
    }
    else
    {
        /* 缓冲区已满，从头开始覆盖接收 */
        g_esp_rx_cnt = 0;
        HAL_UARTEx_ReceiveToIdle_IT(&huart2,
                                    (uint8_t*)g_esp_rx_buf,
                                    ESP_BUF_SIZE);
    }
}
 
/**
 * @brief 清空接收缓冲区
 *
 * 清空缓冲区并重新启动空闲中断接收
 */
void esp_clear_rx_buf(void)
{
    /* 先中止当前接收，确保 UART 回到 READY 状态 */
    HAL_UART_Abort_IT(&huart2);
 
    /* 禁用中断保护 */
    __disable_irq();
 
    /* 清空接收缓冲区 */
    memset((void*)g_esp_rx_buf, 0, sizeof(g_esp_rx_buf));
 
    /* 重置接收计数和标志 */
    g_esp_rx_cnt = 0;
    g_esp_rx_end = 0;
 
    /* 启用中断 */
    __enable_irq();
 
    /* 重新启动空闲中断接收（此时 UART 已 READY，不会返回 HAL_BUSY） */
    HAL_UARTEx_ReceiveToIdle_IT(&huart2, (uint8_t*)g_esp_rx_buf, ESP_BUF_SIZE);
}
 
/**
 * @brief 发送AT指令
 *
 * 发送AT指令并清空接收缓冲区
 *
 * @param str AT指令字符串（建议以"\r\n"结尾）
 */
void esp_send_at(const char* str)
{
    /* 参数校验 */
    if (str == NULL)
    {
        return;
    }
 
    /* 清空接收缓冲区 */
    esp_clear_rx_buf();
 
    /* 发送AT指令 */
    HAL_UART_Transmit(&huart2, (uint8_t*)str, strlen(str), HAL_MAX_DELAY);
}
 
 
/* ==================== AT指令集测试函数 ==================== */
 
/**
 * @brief AT指令集测试函数
 *
 * 依次测试常见AT指令，通过串口1 printf打印输出测试结果
 * 测试项目：
 *   1. AT 自检
 *   2. ATE0 关闭回显
 *   3. AT+CWMODE? 查询工作模式
 *   4. AT+CIFSR 查询IP地址
 *   5. AT+CIPMUX? 查询连接模式
 *
 * @return 无
 */
void esp_test1(void)
{
    esp_send_at("AT\r\n");
    delay_ms(1000);
    printf("%s",g_esp_rx_buf);
    
    esp_send_at("ATE0\r\n");
    delay_ms(1000);
    printf("%s",g_esp_rx_buf); 
    
    esp_send_at("AT+CWMODE?\r\n");
    delay_ms(1000);
    printf("%s",g_esp_rx_buf); 
    
    esp_send_at("AT+CIPMUX?\r\n");
    delay_ms(1000);
    printf("%s",g_esp_rx_buf);		
    
    esp_send_at("AT+CIFSR\r\n");
    delay_ms(1000);
    printf("%s",g_esp_rx_buf);		
    
    while(1);
}
```

 

**2.3 esp.h**

```C
/*
 * ============================================================================
 * 文件名称：esp.h
 * 文件描述：ESP12-F Wi-Fi模块驱动头文件
 *
 * 硬件配置：
 *   - 通信接口：USART2（TX: PA2, RX: PA3）
 *   - 波特率：115200bps
 *   - 数据位：8位
 *   - 校验位：无
 *   - 停止位：1位
 *   - AT指令集与ESP8266兼容
 * 日期：2026/05/09
 * ============================================================================
 */
 
#ifndef __ESP_H__
#define __ESP_H__
 
/* ==================== 配置宏定义 ==================== */
 
 
/**
 * @brief 缓冲区大小配置
 * 根据实际需求调整，建议值：256-1024字节
 */
#define ESP_BUF_SIZE    512
 
/**
 * @brief WIFI热点配置
 * 根据实际网络环境修改
 */
#define WIFI_SSID         "TWon"
#define WIFI_PASSWORD     "88888888"
 
/* ==================== 全局变量声明 ==================== */
 
/**
 * @brief 发送缓冲区
 * 用于存储待发送的数据
 */
extern uint8_t g_esp_tx_buf[ESP_BUF_SIZE];
 
/**
 * @brief 接收缓冲区（修饰，中断中使用）
 * 用于存储从ESP模块接收到的数据
 */
extern  uint8_t g_esp_rx_buf[ESP_BUF_SIZE];
 
/**
 * @brief 接收数据计数（修饰，中断中使用）
 * 记录当前接收缓冲区中的字节数
 */
extern  uint32_t g_esp_rx_cnt;
 
/**
 * @brief 接收完成标志（修饰，中断中使用）
 * 1: 接收完成，0: 接收中
 */
extern  uint32_t g_esp_rx_end;
 
 
 
/* ==================== 函数原型声明 ==================== */
 
/**
 * @brief 初始化ESP模块
 *
 * 初始化USART2串口通信，配置波特率为115200bps，启动空闲中断接收
 *
 * @return 无
 */
extern void esp_init(void);
 
 
/**
 * @brief 发送AT指令
 *
 * 发送AT指令并清空接收缓冲区
 *
 * @param str AT指令字符串（建议以"\r\n"结尾）
 *
 * @return 无
 */
extern void esp_send_at(const char* str);
 
/**
 * @brief 清空接收缓冲区
 *
 * @return 无
 */
extern void esp_clear_rx_buf(void);
 
/**
 * @brief 串口2接收事件处理函数（由HAL回调函数调用）
 *
 * @param size 本次实际接收到的字节数
 */
extern void esp_rx_event_handler(uint16_t size);
 
/**
 * @brief AT指令集测试函数
 *
 * 依次测试常见AT指令，通过串口1 printf打印输出测试结果
 *
 * @return 无
 */
extern void esp_test1(void);
 
#endif /* __ESP_H__ */
 
```

 

**2.4 ISR_callback.c**

 

 

**2.5 测试函数**

```C
/**
 * @file    isr_callback.c
 * @brief   HAL 中断回调函数集合
 *
 * 回调分发说明：
 *   1. USART1（调试串口）：接收事件上报给应用层，由应用层处理；
 *   2. USART2（ESP12-F Wi-Fi模块）：接收事件转发给 ESP 驱动模块处理，
 *      由 esp_rx_event_handler() 完成数据保存与下次接收启动。
 */
#include "main.h"
#include "esp.h"
 
/* 引用应用层定义的串口1接收标志 */
extern uint32_t g_uart1_rx_cnt;
extern uint32_t g_uart1_rx_end;
 
/**
 * @brief  UART 接收完成回调（接收满指定长度数据时由 HAL 调用）
 *
 * @param  huart 触发回调的串口句柄
 */
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
 /* USART1 接收完成：置位接收结束标志，供应用层轮询 */
 if (huart->Instance == USART1)
	g_uart1_rx_end = 1;
}
 
/**
 * @brief  UART 接收事件回调（收到指定长度或检测到空闲时由 HAL 调用）
 *
 * @param  huart 触发回调的串口句柄
 * @param  Size  本次实际接收到的字节数
 */
void HAL_UARTEx_RxEventCallback(UART_HandleTypeDef *huart, uint16_t Size)
{
 /* USART1 接收事件：记录字节数并置位结束标志，供应用层轮询 */
 if (huart->Instance == USART1) {
		g_uart1_rx_cnt = Size;
		g_uart1_rx_end = 1;
 }
 
 /* USART2 连接 ESP12-F Wi-Fi模块：交给 ESP 驱动模块处理 */
 if (huart->Instance == USART2)
		esp_rx_event_handler(Size);
}
 
```

 

**3. 现象**

![img](./img/56614.png)



# MQTT

### **一、MQTT概述**

MQTT是一种轻量级的、基于发布/订阅模式的消息传输协议，专门为低带宽、高延迟或不可靠的网络环境设计。它最初由IBM和Arduino的联合创始人Andy Stanford-Clark在1999年创建，如今已成为物联网（IoT） 领域上的标准通信协议。

### **二、MQTT协议版本**

MQTT 有两个主流官方版本：**3.1.1（最常用）** 和 **5.0（最新）**，以下为 OASIS 发布的权威文档地址。

| 版本                           | 文档类型    | 官方下载链接                                                 |
| ------------------------------ | ----------- | ------------------------------------------------------------ |
| （OASIS 标准，2014-10-29）     | PDF         | http://docs.oasis-open.org/mqtt/mqtt/v3.1.1/os/mqtt-v3.1.1-os.pdf |
|                                | HTML        | http://docs.oasis-open.org/mqtt/mqtt/v3.1.1/os/mqtt-v3.1.1-os.html |
|                                | DOC         | http://docs.oasis-open.org/mqtt/mqtt/v3.1.1/os/mqtt-v3.1.1-os.doc |
| MQTT 5.0（OASIS 标准，最新版） | PDF（推荐） | https://docs.oasis-open.org/mqtt/mqtt/v5.0/mqtt-v5.0.pdf     |
|                                | HTML        | https://docs.oasis-open.org/mqtt/mqtt/v5.0/mqtt-v5.0.html    |

👍推荐MQTT 3.1.1中文版本：https://github.com/mcxiaoke/mqtt/blob/master/protocol/MQTT-3.1.1-CN.pdf﻿

### **三、MQTT协议核心**

![img](./img/56571.png)

 

1. 连接：客户端与 MQTT 服务器完成认证并建立可靠通信链路的过程。

2. 主题：消息的 “虚拟通道”，发布者向主题发消息，订阅者通过主题收消息，实现发布与订阅的解耦。

3. 订阅：客户端（订阅者）向服务器声明 “我要接收某个主题的所有消息”，后续该主题有新消息时服务器会主动推送。

4. 发布：客户端（发布者）将消息发送到指定主题，由服务器负责分发给所有订阅了该主题的设备。

5. 心跳：客户端与服务器之间定期发送的小型探测包，用来维持连接、确认对方在线，避免无效空闲连接被断开。

**（注意：同一个设备，既可以为订阅者，也可以为发布者）**

### **四、巴法云支持的MQTT服务质量等级**

巴法云MQTT 支持Qos0 Qos1，支持retian保留消息，不支持qos2，使用qos2会被强制下线，次数过多可造成账号异常无法使用。

MQTT的保留消息（Retained Message）就像是MQTT服务器为每个主题“记住”的**最后一条重要消息**。当有新设备订阅这个主题时，服务器会立刻把这条“记忆”发给它，省去了等待新消息的麻烦。在物联网场景中，一个温度传感器可能每小时才上报一次数据。如果控制端在两次上报的间隙启动，订阅后就能立刻获取到传感器最后上报的温度值，而不必等到下一次更新

MQTT的QoS（Quality of Service，服务质量等级） 是为解决不稳定网络下的消息传递可靠性设计的核心机制，定义了发布者（Publisher）向订阅者（Subscriber）传递消息 的保障能力。

#### **1.QoS 0**

QoS 0等级，消息最多被传递 1 次，最终结果只有两种：接收端成功收到 / 接收端完全丢失。实现机制是 MQTT **最轻量化**的传输方式，全程仅 1 次消息交互，**无任何确认**、重试逻辑：

💡

① 发布者直接向 Broker（消息服务器）发送 PUBLISH 消息；

② Broker 收到后直接转发给订阅者，不要求接收端返回任何应答；

③ 发布者发送后立刻结束流程，无论消息是否送达，都不会重发

![img](./img/56572.png)

#### **2.QoS 1**

QoS 1等级，消息至少会被传递 1 次，最终结果只有两种：接收端成功收到 1 次 / 接收端收到多次（重复）。 实现机制核心是 发送 - 确认 - 重发 闭环，通过2 次消息交互 实现可靠交付，解决消息丢失问题：

💡

① 发布者向 Broker 发送 PUBLISH 消息，并缓存该消息（不销毁）；

② Broker 成功接收后，向发布者返回 PUBACK 确认报文；

③ 发布者收到 PUBACK，则销毁缓存的消息，流程结束；

④ 若发布者超时未收到 PUBACK（网络丢包 / 超时），则无限重试发送 该消息，直到收到确认。

❗ 重复原因：PUBACK 报文丢失（消息已送达，但确认包丢了），发布者会误以为消息未送达而重发，导致接收端收到多条相同消息。

![img](./img/56573.png)



#### **3.QoS 2**

QoS 2等级，消息精准被传递且仅传递 1 次，最终结果唯一：接收端仅收到 1 次消息。实现机制核心是，通过4 次消息交互 的「双向完整握手」，同时解决「丢失」和「重复」两大问题，实现「精准一次交付」，也是唯一能做到不丢不重 的级别：

💡

① 发布者发送 PUBLISH 消息，Broker 接收后返回 PUBREC 报文（确认「消息已接收，待处理」）；

② 发布者收到 PUBREC，销毁原消息、缓存 PUBREC 回执，并向 Broker 发送 PUBREL 报文（确认「收到你的回执，可正式处理消息」）；

③ Broker 收到 PUBREL，将消息转发给订阅者，完成消息投递后，向发布者返回 PUBCOMP 报文（确认「消息已成功投递，流程结束」）；

④ 发布者收到 PUBCOMP，销毁所有缓存，全程流程结束；

⑤ 任意环节超时，均触发重试，且通过 MQTT 内置的「消息 ID」机制，确保重试的消息可被幂等识别，最终仅生效 1 次。

![img](./img/56574.png)

### **五、软件测试接入巴法云**

﻿[接入文档](https://cloud.bemfa.com/docs/src/mqtt.html)﻿

**1. 用户在**[MQTT控制台](https://cloud.bemfa.com/tcp/devicemqtt.html)**新建主题**

![img](./img/56575.png)

 

**2. 软件使用连接巴法云**

**2.1 连接与订阅**

![img](./img/56576.png)

![img](./img/56577.png)

**2.2 发布**

![img](./img/56578.png)

 

![img](./img/56579.png)

**3. 推送注意事项**

![img](./img/56580.png)

### **六、MQTT协议报文分析（QoS 0）**

此前我们已通过 MQTT.fx 成功连接巴法云平台，完整掌握了 MQTT 的基础使用流程。接下来，我们将深入剖析 MQTT 协议报文的结构与细节，为后续自主编码实现 MQTT 在实际项目中的落地与应用奠定坚实基础。

**控制报文（MQTT Control Packet） ：通过网络连接发送的信息数据包。**

#### **1. MQTT报文格式**

![img](./img/56581.png)

 

#### **2.** **连接服务端CONNECT）**

该报文由客户端发送Broker（服务器 / 代理），作用：客户端与 Broker 完成身份校验、参数协商，建立 MQTT 级别的会话连接。

**2.1 固定报头**

![img](./img/56582.png)

**注意：MQTT的剩余长度使用变长编码，规则如下：**

- 每个字节使用7位（第0~6位）存储实际数据，第7位（最高位）作为连续标志

- 连续标志=1：表示还有后续字节

- 连续标志=0：表示这是最后一个字节

- 最多4个字节，可表示最大长度：128^(4)-1 = 268,435,455字节

- 长度低字节在前，高字节在后

**2.2 可变报头**

![img](./img/56583.png)

 

**2.3 有效载荷**

![img](./img/56584.png)

 

#### **3.**  **确认连接请求（CONNACK）**

该报文由Broker（服务器 / 代理）发送到客户端，作用：Broker响应从客户端收到的 CONNECT 报文，告诉客户端连接的结果。

**3.1 固定报头**

![img](./img/56585.png)

**3.2 可变报头**

![img](./img/56586.png)

 

**连接返回码如下：**

![img](./img/56587.png)

**3.3 有效载荷**

CONNACK 报文没有有效载荷。

 

#### **4. 订阅主题（SUBSCRIBE）**

订阅者通过 SUBSCRIBE 报文向 Broker 注册主题兴趣后，Broker 会维护「客户端 - 主题 - QoS」的映射关系。

当有发布者（例如，Broker）通过 PUBLISH 报文向匹配订阅主题的 Topic 发送消息时，Broker 会根据映射关系，将消息推送给所有符合条件的订阅客户端。

**4.1 固定报头**

![img](./img/56588.png)

**4.2 可变报头**

![img](./img/56589.png)

**4.3** **有效载荷**

![img](./img/56590.png)

 

有效载荷示例：

![img](./img/56591.png)

#### **5. 订阅确认（SUBACK ）**

服务端发送 SUBACK 报文给客户端， 用于确认它已收到并且正在处理 SUBSCRIBE 报文

**5.1 固定报头**

![img](./img/56592.png)

 

**5.2 可变报头**

![img](./img/56593.png)

 

**5.3 有效载荷**

![img](./img/56594.png)

  

#### **6.** **发布消息（PUBLISH）**

PUBLISH 控制报文是指从客户端向 Broker 服务端或者 Broker 服务端向客户端传输一个应用消息。

**6.1 固定报头**

![img](./img/56595.png)

**6.2 可变报头**

![img](./img/56596.png)

**6.3 有效载荷**

有效载荷包含将被发布的应用消息。 数据的内容和格式是应用特定的。 有效载荷的长度这样计算： 用固定报头中的剩余长度字段的值减去可变报头的长度。 包含零长度有效载荷的 PUBLISH 报文是合法的。

 

#### **7.**  **心跳请求（PINGREQ）**

客户端发送 PINGREQ 报文给服务端的。用于：

💡

1. 在没有任何其它控制报文从客户端发给服务的时，告知服务端客户端还活着。
2. 请求服务端发送 响应确认它还活着。
3. 使用网络以确认网络连接没有断开。

**注意：心跳请求的报文间隔时间，必须 小于 在连接服务端时设置的保持时间。**

**7.1 固定报头**

![img](./img/56597.png)

**7.2 可变报头**

无

**7.3 有效载荷**

无

#### **8. 心跳响应（PINGRESP）**

服务端发送 PINGRESP 报文响应客户端的 PINGREQ 报文。 表示服务端还活着。

**8.1 固定报头**

![img](./img/56598.png)

**8.2 可变报头**

无

**8.3 有效载荷**

无

### **七、MQTT 连接示例代码**

#### **1. esp_mqtt.c关键函数**

```C
/*
 * ============================================================================
 * 文件名称：esp_mqtt.c
 * 文件描述：ESP12-F MQTT客户端实现文件（巴法云版本）
 *
 * 功能描述：
 *   本文件实现了基于ESP12-F模块的MQTT客户端功能，支持巴法云平台接入。
 *   包含MQTT连接、订阅、发布、心跳等核心功能。
 *
 * MQTT协议实现：
 *   - 协议版本：MQTT 3.1.1
 *   - 支持QoS 0消息发布
 *   - 支持心跳保活机制（60秒）
 *   - 透传模式下的数据收发
 *
 * 实现流程：
 *   1. ESP模块初始化（退出透传、复位、关闭回显）
 *   2. 连接WiFi热点
 *   3. 连接MQTT服务器（TCP连接）
 *   4. 进入透传模式
 *   5. MQTT协议握手（CONNECT）
 *   6. 订阅主题（SUBSCRIBE）
 *   7. 定时发送心跳包（PINGREQ）
 *   8. 发布数据（PUBLISH）
 *
 * 硬件说明：
 *   - ESP12-F连接在USART2（TX: PA2, RX: PA3）
 *   - 波特率：115200bps
 *   - AT指令集与ESP8266完全兼容
 *   - 使用HAL_UARTEx_ReceiveToIdle_IT实现空闲中断接收
 *   - 调试输出通过USART1（PA9/PA10）printf打印
 *
 * ============================================================================
 */
 
#include "main.h"
#include <string.h>
#include <stdlib.h>
#include <stdio.h>
#include "delay.h"
#include "usart.h"
#include "esp.h"
#include "esp_mqtt.h"
 
/* ==================== MQTT协议固定数据包定义 ==================== */
 
/**
 * @brief MQTT CONNECT ACK响应包
 * 服务器收到CONNECT请求后返回的成功响应
 * 格式：0x20(类型) + 0x02(长度) + 0x00 0x00(返回码，0表示成功)
 */
const uint8_t g_packet_connect_ack[4] = {0x20, 0x02, 0x00, 0x00};
 
/**
 * @brief MQTT DISCONNECT数据包
 * 客户端主动断开连接时发送
 * 格式：0xe0(类型) + 0x00(长度)
 */
const uint8_t g_packet_disconnect[2] = {0xe0, 0x00};
 
/**
 * @brief MQTT PINGREQ心跳包
 * 客户端向服务器发送心跳，保持连接活跃
 * 格式：0xc0(类型) + 0x00(长度)
 */
const uint8_t g_packet_heart[2] = {0xc0, 0x00};
 
/**
 * @brief MQTT PINGRESP心跳响应包
 * 服务器对PINGREQ的响应
 * 格式：0xd0(类型) + 0x00(长度)
 */
const uint8_t g_packet_heart_reply[2] = {0xd0, 0x00};
 
/**
 * @brief MQTT SUBSCRIBE ACK响应包
 * 服务器对SUBSCRIBE请求的响应
 * 格式：0x90(类型) + 0x03(长度)
 */
const uint8_t g_packet_sub_ack[2] = {0x90, 0x03};
 
/* ==================== 全局变量定义 ==================== */
 
/**
 * @brief MQTT消息缓冲区
 * 用于存储待发布的消息内容
 */
char g_mqtt_msg[526];
 
/**
 * @brief MQTT发送数据长度
 * 记录当前正在组装的MQTT数据包长度
 */
uint32_t g_mqtt_tx_len;
 
/* ==================== MQTT基础函数实现 ==================== */
 
/**
 * @brief MQTT发送数据
 * 将数据通过ESP模块发送到MQTT服务器
 * @param buf 数据缓冲区指针
 * @param len 数据长度
 */
void mqtt_send_bytes(uint8_t *buf, uint32_t len)
{
    esp_send_bytes(buf, len);
}
 
/**
 * @brief 发送MQTT心跳包
 * 向服务器发送PINGREQ，保持连接活跃
 */
void mqtt_send_heart(void)
{
    mqtt_send_bytes((uint8_t *)g_packet_heart, sizeof(g_packet_heart));
}
 
/**
 * @brief MQTT断开连接
 * 发送DISCONNECT数据包，主动断开与服务器的连接
 */
void mqtt_disconnect(void)
{
    mqtt_send_bytes((uint8_t *)g_packet_disconnect, sizeof(g_packet_disconnect));
}
 
/**
 * @brief MQTT协议初始化
 * 清空收发缓冲区并发送断开连接包，确保处于干净状态
 * @param prx 接收缓冲区指针（未使用）
 * @param rxlen 接收缓冲区长度（未使用）
 * @param ptx 发送缓冲区指针（未使用）
 * @param txlen 发送缓冲区长度（未使用）
 */
void mqtt_init(uint8_t *prx, uint16_t rxlen, uint8_t *ptx, uint16_t txlen)
{
    /* 清空ESP发送缓冲区 */
    memset(g_esp_tx_buf, 0, sizeof(g_esp_tx_buf));
    /* 清空ESP接收缓冲区 */
    memset((void *)g_esp_rx_buf, 0, sizeof(g_esp_rx_buf));
 
    /* 发送两次断开连接包，确保断开干净 */
    mqtt_disconnect();
    delay_ms(100);
    mqtt_disconnect();
    delay_ms(100);
}
 
/**
 * @brief MQTT连接服务器（打包并发送CONNECT数据包）
 *
 * 构建并发送MQTT CONNECT数据包，建立客户端与MQTT服务器的连接。
 * CONNECT是MQTT协议中客户端发起连接的第一个数据包，必须在完成TCP连接后发送。
 * 服务器收到CONNECT后会验证客户端身份，并返回CONNACK响应。
 *
 * CONNECT数据包结构：
 *   固定报头：0x10 + 剩余长度（变长编码）
 *   可变报头：协议名("MQTT") + 协议级别(0x04) + 连接标志(0x02) + 心跳间隔(60秒)
 *   Payload：客户端ID长度 + 客户端ID内容
 *
 * CONNACK响应：
 *   0x20 0x02 + 会话标志 + 返回码（0x00=成功）
 *
 * 连接标志0x02 = Clean Session=1，不使用遗嘱、用户名密码
 *
 * @param client_id 客户端标识符（巴法云使用UID）
 * @param user_name 用户名（巴法云可为空）
 * @param password 密码（巴法云可为空）
 * @return 0=连接成功，-1=连接失败
 */
int32_t mqtt_connect(char *client_id, char *user_name, char *password)
{
    uint32_t client_id_len = strlen(client_id);
    uint32_t user_name_len = strlen(user_name);
    uint32_t password_len = strlen(password);
    uint32_t data_len;
    uint32_t cnt = 2;  /* 重试次数 */
    uint32_t wait = 0;
 
    g_mqtt_tx_len = 0;
 
    /* 计算数据包长度：可变报头(10) + Payload */
    data_len = 10 + (client_id_len + 2);
    if (user_name_len > 0)
    {
        data_len += (user_name_len + 2);
    }
    if (password_len > 0)
    {
        data_len += (password_len + 2);
    }
 
    /* 构建固定报头：报文类型 */
    g_esp_tx_buf[g_mqtt_tx_len++] = 0x10;
 
    /* 剩余长度（变长编码） */
    do
    {
        uint8_t encodedByte = data_len % 128;
        data_len = data_len / 128;
        if (data_len > 0)
        {
            encodedByte |= 128;
        }
        g_esp_tx_buf[g_mqtt_tx_len++] = encodedByte;
    } while (data_len > 0);
 
    /* 构建可变报头 */
    /* 协议名长度 */
    g_esp_tx_buf[g_mqtt_tx_len++] = 0;
    g_esp_tx_buf[g_mqtt_tx_len++] = 4;
    /* 协议名 "MQTT" */
    g_esp_tx_buf[g_mqtt_tx_len++] = 'M';
    g_esp_tx_buf[g_mqtt_tx_len++] = 'Q';
    g_esp_tx_buf[g_mqtt_tx_len++] = 'T';
    g_esp_tx_buf[g_mqtt_tx_len++] = 'T';
    /* 协议级别 MQTT 3.1.1 */
    g_esp_tx_buf[g_mqtt_tx_len++] = 4;
    /* 连接标志：Clean Session=1 */
    g_esp_tx_buf[g_mqtt_tx_len++] = 0x02;
    /* 心跳间隔：60秒 */
    g_esp_tx_buf[g_mqtt_tx_len++] = 0;
    g_esp_tx_buf[g_mqtt_tx_len++] = 60;
 
    /* 构建Payload：客户端ID */
    g_esp_tx_buf[g_mqtt_tx_len++] = BYTE1(client_id_len);
    g_esp_tx_buf[g_mqtt_tx_len++] = BYTE0(client_id_len);
    memcpy(&g_esp_tx_buf[g_mqtt_tx_len], client_id, client_id_len);
    g_mqtt_tx_len += client_id_len;
 
    /* 用户名字段（可选） */
    if (user_name_len > 0)
    {
        g_esp_tx_buf[g_mqtt_tx_len++] = BYTE1(user_name_len);
        g_esp_tx_buf[g_mqtt_tx_len++] = BYTE0(user_name_len);
        memcpy(&g_esp_tx_buf[g_mqtt_tx_len], user_name, user_name_len);
        g_mqtt_tx_len += user_name_len;
    }
 
    /* 密码字段（可选） */
    if (password_len > 0)
    {
        g_esp_tx_buf[g_mqtt_tx_len++] = BYTE1(password_len);
        g_esp_tx_buf[g_mqtt_tx_len++] = BYTE0(password_len);
        memcpy(&g_esp_tx_buf[g_mqtt_tx_len], password, password_len);
        g_mqtt_tx_len += password_len;
    }
 
    /* 发送CONNECT并等待CONNACK响应，最多重试2次 */
    while (cnt--)
    {
        /* 必须调用esp_clear_rx_buf()重启UART接收（HAL库特性） */
        esp_clear_rx_buf();
 
        mqtt_send_bytes(g_esp_tx_buf, g_mqtt_tx_len);
 
        wait = 3000;
        while (wait--)
        {
            uint16_t i;
            delay_ms(1);
 
            /* 在整个接收缓冲区中搜索CONNACK响应（0x20 0x02） */
            for (i = 0; i + 1 < g_esp_rx_cnt; i++)
            {
                if ((g_esp_rx_buf[i] == g_packet_connect_ack[0]) &&
                    (g_esp_rx_buf[i + 1] == g_packet_connect_ack[1]))
                {
                    return 0;  /* 连接成功 */
                }
            }
        }
    }
 
    return -1;  /* 连接失败 */
}
 
/**
 * @brief MQTT订阅/取消订阅主题
 *
 * 构建并发送MQTT SUBSCRIBE（订阅）或UNSUBSCRIBE（取消订阅）数据包。
 *
 * SUBSCRIBE数据包结构：
 *   固定报头：0x82(SUBSCRIBE) / 0xA2(UNSUBSCRIBE) + 剩余长度
 *   可变报头：报文标识符（2字节，固定0x0001）
 *   Payload：主题名长度 + 主题名 + QoS等级（仅订阅时）
 *
 * SUBACK响应：0x90 0x03 + 报文标识符 + 授予的QoS
 *
 * @param topic    主题名称
 * @param qos      QoS等级（0或1）
 * @param whether  1=订阅，0=取消订阅
 * @return 0=成功，-1=失败
 */
int32_t mqtt_subscribe_topic(char *topic, uint8_t qos, uint8_t whether)
{
    uint32_t cnt = 2;
    uint32_t wait = 0;
    uint32_t topiclen = strlen(topic);
 
    /* 计算数据包长度 */
    uint32_t data_len = 2 + (topiclen + 2) + (whether ? 1 : 0);
 
    g_mqtt_tx_len = 0;
 
    /* 构建固定报头 */
    if (whether)
    {
        g_esp_tx_buf[g_mqtt_tx_len++] = 0x82;  /* SUBSCRIBE */
    }
    else
    {
        g_esp_tx_buf[g_mqtt_tx_len++] = 0xA2;  /* UNSUBSCRIBE */
    }
 
    /* 剩余长度（变长编码） */
    do
    {
        uint8_t encodedByte = data_len % 128;
        data_len = data_len / 128;
        if (data_len > 0)
        {
            encodedByte |= 128;
        }
        g_esp_tx_buf[g_mqtt_tx_len++] = encodedByte;
    } while (data_len > 0);
 
    /* 构建可变报头：报文标识符 */
    g_esp_tx_buf[g_mqtt_tx_len++] = 0;
    g_esp_tx_buf[g_mqtt_tx_len++] = 0x01;
 
    /* 构建Payload：主题名 */
    g_esp_tx_buf[g_mqtt_tx_len++] = BYTE1(topiclen);
    g_esp_tx_buf[g_mqtt_tx_len++] = BYTE0(topiclen);
    memcpy(&g_esp_tx_buf[g_mqtt_tx_len], topic, topiclen);
    g_mqtt_tx_len += topiclen;
 
    /* 订阅时添加QoS等级 */
    if (whether)
    {
        g_esp_tx_buf[g_mqtt_tx_len++] = qos;
    }
 
    /* 发送并等待SUBACK响应，最多重试2次 */
    while (cnt--)
    {
        /* 必须调用esp_clear_rx_buf()重启UART接收（HAL库特性） */
        esp_clear_rx_buf();
 
        mqtt_send_bytes(g_esp_tx_buf, g_mqtt_tx_len);
 
        wait = 3000;
        while (wait--)
        {
            uint16_t i;
            delay_ms(1);
 
            /* 在整个接收缓冲区中搜索SUBACK响应（0x90 0x03） */
            for (i = 0; i + 1 < g_esp_rx_cnt; i++)
            {
                if ((g_esp_rx_buf[i] == g_packet_sub_ack[0]) &&
                    (g_esp_rx_buf[i + 1] == g_packet_sub_ack[1]))
                {
                    return 0;  /* 订阅成功 */
                }
            }
        }
    }
 
    return -1;  /* 订阅失败 */
}
 
/**
 * @brief MQTT发布消息
 *
 * 构建并发送MQTT PUBLISH数据包，向服务器发布消息。
 *
 * PUBLISH数据包结构：
 *   固定报头：0x30 + 剩余长度（变长编码）
 *   可变报头：主题名长度 + 主题名 + [报文标识符（QoS>0时）]
 *   Payload：消息内容
 *
 * @param topic   主题名称
 * @param message 消息内容
 * @param qos     QoS等级（0或1）
 * @return 发送的字节数
 */
uint32_t mqtt_publish_data(char *topic, char *message, uint8_t qos)
{
    static uint16_t id = 0;  /* 报文标识符，QoS>0时使用 */
 
    uint32_t topicLength = strlen(topic);
    uint32_t messageLength = strlen(message);
    uint32_t data_len;
    uint8_t encodedByte;
 
    g_mqtt_tx_len = 0;
 
    /* 计算剩余长度 */
    if (qos)
    {
        data_len = (2 + topicLength) + 2 + messageLength;
    }
    else
    {
        data_len = (2 + topicLength) + messageLength;
    }
 
    /* 构建固定报头 */
    g_esp_tx_buf[g_mqtt_tx_len++] = 0x30;
 
    /* 剩余长度（变长编码） */
    do
    {
        encodedByte = data_len % 128;
        data_len = data_len / 128;
        if (data_len > 0)
        {
            encodedByte |= 128;
        }
        g_esp_tx_buf[g_mqtt_tx_len++] = encodedByte;
    } while (data_len > 0);
 
    /* 构建可变报头：主题名 */
    g_esp_tx_buf[g_mqtt_tx_len++] = BYTE1(topicLength);
    g_esp_tx_buf[g_mqtt_tx_len++] = BYTE0(topicLength);
    memcpy(&g_esp_tx_buf[g_mqtt_tx_len], topic, topicLength);
    g_mqtt_tx_len += topicLength;
 
    /* QoS>0时添加报文标识符 */
    if (qos)
    {
        g_esp_tx_buf[g_mqtt_tx_len++] = BYTE1(id);
        g_esp_tx_buf[g_mqtt_tx_len++] = BYTE0(id);
        id++;
    }
 
    /* 构建Payload：消息内容 */
    memcpy(&g_esp_tx_buf[g_mqtt_tx_len], message, messageLength);
    g_mqtt_tx_len += messageLength;
 
    /* 发送数据包 */
    mqtt_send_bytes(g_esp_tx_buf, g_mqtt_tx_len);
 
    return g_mqtt_tx_len;
}
 
/* ==================== ESP MQTT初始化函数 ==================== */
 
/**
 * @brief ESP MQTT完整初始化函数
 *
 * 初始化流程：
 *   1. ESP模块基础初始化
 *   2. 退出透传模式
 *   3. 复位ESP模块
 *   4. 关闭回显
 *   5. 连接WiFi热点
 *   6. 建立TCP连接到巴法云MQTT服务器
 *   7. 进入透传模式
 *   8. MQTT协议连接（CONNECT）
 *   9. 订阅主题（SUBSCRIBE）
 *
 * @return 0=成功，负数=失败
 */
int32_t esp_mqtt_init(void)
{
    int32_t rt;
 
    /* 步骤1: ESP模块基础初始化 */
    esp_init();
 
    /* 步骤2: 退出透传模式 */
    rt = esp_exit_transparent_transmission();
    if (rt)
    {
        printf("esp_exit_transparent_transmission fail\r\n");
        return -1;
    }
    printf("esp_exit_transparent_transmission success\r\n");
    delay_ms(2000);
 
    /* 步骤3: 复位ESP模块 */
    rt = esp_reset();
    if (rt)
    {
        printf("esp_reset fail\r\n");
        return -2;
    }
    delay_ms(2000);
 
    /* 步骤4: 关闭回显 */
    rt = esp_enable_echo(0);
    if (rt)
    {
        printf("esp_enable_echo(0) fail\r\n");
        return -3;
    }
    printf("esp_enable_echo(0) success\r\n");
    delay_ms(2000);
 
    /* 步骤5: 连接WiFi热点 */
    rt = esp_connect_ap(WIFI_SSID, WIFI_PASSWORD);
    if (rt)
    {
        printf("esp_connect_ap fail\r\n");
        return -4;
    }
    printf("esp_connect_ap success\r\n");
    delay_ms(2000);
 
    /* 步骤6: 连接巴法云MQTT服务器 */
    rt = esp_connect_server(ESP_PROTOCOL_TCP, MQTT_BROKERADDRESS, 9501);
    if (rt)
    {
        printf("esp_connect_server fail\r\n");
        return -5;
    }
    printf("esp_connect_server success\r\n");
    delay_ms(2000);
 
    /* 步骤7: 进入透传模式 */
    rt = esp_entry_transparent_transmission();
    if (rt)
    {
        printf("esp_entry_transparent_transmission fail\r\n");
        return -6;
    }
    printf("esp_entry_transparent_transmission success\r\n");
    delay_ms(2000);
 
    /* 步骤8: MQTT协议连接 */
    if (mqtt_connect(MQTT_CLIENTID, MQTT_USARNAME, MQTT_PASSWD))
    {
        printf("mqtt_connect fail\r\n");
        return -7;
    }
    printf("mqtt_connect success\r\n");
    delay_ms(2000);
 
    /* 步骤9: 订阅主题 */
    if (mqtt_subscribe_topic(BAFA_TOPIC_LED, 0, 1))
    {
        printf("mqtt_subscribe_topic BAFA_TOPIC_LED fail\r\n");
        return -8;
    }
 
    if (mqtt_subscribe_topic(BAFA_TOPIC_HUMITURE, 0, 1))
    {
        printf("mqtt_subscribe_topic BAFA_TOPIC_HUMITURE fail\r\n");
        return -9;
    }
    printf("mqtt_subscribe_topic success\r\n");
 
    return 0;
}
```

 

#### **2.**  **esp_mqtt.c中的测试代码**

```C
/* ==================== ESP MQTT测试函数 ==================== */
 
/**
 * @brief ESP MQTT测试函数
 *
 * 测试流程：
 *   1. 初始化ESP模块并连接巴法云MQTT服务器
 *   2. 发布测试数据到温湿度主题
 *   3. while(1)循环接收服务器消息并打印
 *   4. 定时发送心跳保活（60秒）
 *   5. 收到"end"退出循环
 *
 * MQTT PUBLISH消息解析：
 *   Byte 0: 消息类型 (0x30 = PUBLISH)
 *   Byte 1: 剩余长度
 *   Byte 2-3: 主题长度（大端序）
 *   Byte 4-N: 主题名
 *   Byte N+1-...: 消息内容
 */
void esp_mqtt_test1(void)
{
    uint32_t last_heart_tick = 0;     /* 上次心跳时间（HAL_GetTick） */
    uint32_t last_publish_tick = 0;   /* 上次发布时间（HAL_GetTick） */
    uint16_t payload_offset;          /* 消息内容起始位置 */
    char topic[32] = {0};             /* 主题缓冲区 */
 
    printf("\r\n");
    printf("========================================\r\n");
    printf("      ESP12-F 巴法云MQTT测试\r\n");
    printf("========================================\r\n");
    printf("\r\n");
 
    /* [1] 初始化并连接巴法云MQTT服务器 */
    printf("[1] 初始化ESP模块并连接巴法云MQTT服务器...\r\n");
    while (esp_mqtt_init())
    {
        printf("esp_mqtt_init ...重试\r\n");
        delay_ms(1000);
    }
    printf("    [OK] 连接巴法云成功\r\n");
    printf("\r\n");
 
    /* [2] 发布测试数据到温湿度主题 */
    printf("[2] 发布测试数据到主题 %s...\r\n", MQTT_PUBLISH_TOPIC);
    sprintf(g_mqtt_msg, "#25.5#60.0");
    mqtt_publish_data(MQTT_PUBLISH_TOPIC, g_mqtt_msg, 0);
    printf("    发布数据: %s\r\n", g_mqtt_msg);
    printf("\r\n");
 
    /* [3] 发布LED状态到LED主题 */
    printf("[3] 发布LED状态到主题 %s...\r\n", BAFA_TOPIC_LED);
    sprintf(g_mqtt_msg, "on");
    mqtt_publish_data(BAFA_TOPIC_LED, g_mqtt_msg, 0);
    printf("    发布数据: %s\r\n", g_mqtt_msg);
    printf("\r\n");
 
    printf("========================================\r\n");
    printf("      进入消息接收循环（发送\"end\"退出）\r\n");
    printf("========================================\r\n");
 
    /* [4] while(1)循环：接收消息、发送心跳 */
    while (1)
    {
        /* 检查是否收到ESP透传数据 */
        if (g_esp_rx_end && g_esp_transparent_transmission_sta)
        {
            /*
             * 一次接收可能包含多个MQTT数据包（粘包），需要逐包解析。
             * 常见类型：
             *   0x90 = SUBACK（订阅响应）
             *   0x30 = PUBLISH（发布消息）
             *   0xD0 = PINGRESP（心跳响应）
             */
            uint16_t offset = 0;
 
            while (offset + 1 < g_esp_rx_cnt)
            {
                uint8_t pkt_type = g_esp_rx_buf[offset];
                uint8_t remain_len = g_esp_rx_buf[offset + 1];
 
                /* 检查剩余长度是否合法 */
                if (remain_len == 0 || offset + 2 + remain_len > g_esp_rx_cnt)
                {
                    break;  /* 数据不完整，等待下一帧 */
                }
 
                if (pkt_type == 0x30)
                {
                    /* ===== PUBLISH 消息解析 ===== */
                    uint16_t topic_len = (g_esp_rx_buf[offset + 2] << 8) | g_esp_rx_buf[offset + 3];
                    uint16_t pkt_end = offset + 2 + remain_len;
 
                    printf("\r\n========== MQTT PUBLISH 消息 ==========\r\n");
                    printf("消息类型: 0x%02X (PUBLISH)\r\n", pkt_type);
                    printf("剩余长度: %u\r\n", remain_len);
                    printf("主题长度: %u\r\n", topic_len);
 
                    /* 提取并打印主题名 */
                    uint16_t copy_len = topic_len > 31 ? 31 : topic_len;
                    memset(topic, 0, sizeof(topic));
                    memcpy(topic, (void *)&g_esp_rx_buf[offset + 4], copy_len);
                    topic[copy_len] = '\0';
                    printf("主题名: \"%s\"\r\n", topic);
 
                    /* 计算消息内容起始位置 */
                    payload_offset = offset + 2 + 2 + topic_len;
 
                    /* 提取并打印消息内容 */
                    if (pkt_end > payload_offset)
                    {
                        char *payload = (char *)&g_esp_rx_buf[payload_offset];
                        uint16_t payload_len = pkt_end - payload_offset;
 
                        printf("消息内容: \"");
                        for (uint16_t i = 0; i < payload_len && i < 32; i++)
                        {
                            printf("%c", payload[i]);
                        }
                        printf("\" (长度: %u)\r\n", payload_len);
 
                        /* 检查是否收到退出指令 */
                        if (strstr(payload, "end") != NULL)
                        {
                            printf("    [收到退出指令，退出MQTT测试]\r\n");
                            goto mqtt_exit;
                        }
 
                        /* 检查LED控制指令 */
                        if (strstr(payload, "on"))
                        {
                            printf("    [LED ON 指令]\r\n");
                        }
                        else if (strstr(payload, "off"))
                        {
                            printf("    [LED OFF 指令]\r\n");
                        }
                    }
                    printf("========================================\r\n\r\n");
                }
                else if (pkt_type == 0x90)
                {
                    /* SUBACK，跳过 */
                    printf("[SUBACK] 订阅响应 (offset=%u)\r\n", offset);
                }
                else if (pkt_type == 0xD0)
                {
                    /* PINGRESP，跳过 */
                    printf("[PINGRESP] 心跳响应\r\n");
                }
                else
                {
                    /* 未知类型，打印并跳过 */
                    printf("[未知] 类型: 0x%02X (offset=%u)\r\n", pkt_type, offset);
                }
 
                /* 移动到下一个数据包 */
                offset += 2 + remain_len;
            }
 
            /* 清空接收缓冲区并重启UART接收，准备接收下一帧数据 */
            esp_clear_rx_buf();
        }
 
 
        /* 每60秒发送一次心跳保活 */
        if ((HAL_GetTick() - last_heart_tick) >= 60000)
        {
            mqtt_send_heart();
            printf("[心跳] 已发送PINGREQ\r\n");
            last_heart_tick = HAL_GetTick();
        }
 
        /* 每10秒发布一次温湿度测试数据 */
        if ((HAL_GetTick() - last_publish_tick) >= 10000)
        {
            sprintf(g_mqtt_msg, "#25.5#60.0");
            mqtt_publish_data(MQTT_PUBLISH_TOPIC, g_mqtt_msg, 0);
            printf("[发布] %s -> %s\r\n", g_mqtt_msg, MQTT_PUBLISH_TOPIC);
            last_publish_tick = HAL_GetTick();
        }
    }
 
mqtt_exit:
    /* [5] 退出处理 */
    printf("\r\n[5] 退出MQTT测试...\r\n");
 
    /* 退出透传模式 */
    esp_exit_transparent_transmission();
    printf("    已退出透传模式\r\n");
 
    /* 断开服务器连接 */
    esp_disconnect_server();
    printf("    已断开服务器连接\r\n");
 
    printf("\r\n========================================\r\n");
    printf("      巴法云MQTT测试完成\r\n");
    printf("========================================\r\n");
}
```

 

#### **3. 现象**

**3.1 连接巴法云服务器**

![img](./img/56599.png)

 

![img](./img/56600.png)

**3.2 软件自带的MQTT服务器测试**

![img](./img/56601.png)

 

![img](./img/56602.png)

