# C语言

[toc]



## gcc

### 编译整体流程 - gcc

#### code

- main.c

```c
#include "hello.h"

int main(int argc, char const *argv[])
{
	
	return 0;
}
```



- hello.h

```

```



#### gcc

- 直接编译

```shell
gcc main.c hello.c -o main
```

- -o : output 表示输出，用于指定输出文件



### 预处理

预处理指令转换成编译器可以识别的形式。预处理主要包含宏替换、文件包含、条件编译、注释移除等。

预处理的输出同行是预处理后的源代码文件，它会被保存成一个临时文件，并作为编译器的输入。预处理器处理后的文件通常会比原始文件大，因为它会展开宏和包含其他文件的内容。



#### CMD

```shell
gcc -E hello.c -o hello.i
gcc -E main.c -o main.i
```



- -E : Expand(展开)，指定 gcc 执行预处理操作
- .i ： intermediate（中间的），预处理后的源文件通常以.i作为后缀。



```c
# 0 "main.c"		// 0是特殊标记
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4 // 1表示，接下来的内容是另一个新文件 3，表示是一个系统文件，4表示符合一个标准的，可以使用c++的扩展 c语言预定义系统文件
# 0 "<command-line>" 2
# 1 "main.c"
```



1：表示开始引入一个新文件

2：表示从文件引入回到原文件

3：表示这是系统标准头文件

4：**这段代码遵循 ISO C 标准，被 `#pragma GCC system_header` 包裹**。



### 编译

编译阶段，编译器会将经过预处理的源代码文件转换成汇编代码。这个阶段，编译器会将源代码翻译成机器能够理解的中间代码，包括词法分析、语法分析、语义分析和优化等过程。编译器会检查代码的语法和语义，生成对应的汇编代码。编译阶段是整个编译过程中最复杂和耗时的阶段之一，它对源代码进行了深入的分析和转换，确保了程序的正确性和性能。



#### code

```shell
gcc -S hello.i -o hello.s
gcc -S main.i -o main.s
```



- -S : Source（源代码），指定 gcc 将预处理后的源代码编译为汇编语言。
- .s：Assembly Source（汇编源码），通常编译后的汇编文件以.s作为后缀



### 汇编

汇编阶段是C语言编译过程中最重要的阶段，它将编译器生成的中间代码或汇编代码转换成目标机器的机器语言代码，也就是目标代码。这个阶段由汇编器（Assembler）完成，其主要任务是将汇编指令翻译成目标机器的二进制形式。主要包含以下几个任务：符号解析、指令翻译、地址关联、重定位、代码优化。最终，汇编器会将翻译和处理后的目标代码输出到目标文件中，用于后续的链接和生成可执行程序或共享库文件。



#### code

```shell
gcc -c mian.s -o main.o
gcc -c mian.c -o main.s
```

- -c : Compile or Assemble（编译或汇编），指定gcc将汇编代码翻译为机器码，但不做链接。
- .o : 



#### objdump

```
objdum -s main.o
```



- -s ：  **显示十六进制原始数据**，不是反汇编，所以你看到的是一堆**机器码字节**，不是汇编代码。



```objc
main.o:     file format elf64-x86-64

Contents of section .text:
 0000 f30f1efa 554889e5 4883ec10 897dfc48  ....UH..H....}.H
 0010 8975f0b8 00000000 e8000000 00b80000  .u..............
 0020 0000c9c3                             ....            
Contents of section .comment:
 0000 00474343 3a202855 62756e74 75203133  .GCC: (Ubuntu 13
 0010 2e332e30 2d367562 756e7475 327e3234  .3.0-6ubuntu2~24
 0020 2e303429 2031332e 332e3000           .04) 13.3.0.    
Contents of section .note.gnu.property:
 0000 04000000 10000000 05000000 474e5500  ............GNU.
 0010 020000c0 04000000 03000000 00000000  ................
Contents of section .eh_frame:
 0000 14000000 00000000 017a5200 01781001  .........zR..x..
 0010 1b0c0708 90010000 1c000000 1c000000  ................
 0020 00000000 24000000 00450e10 8602430d  ....$....E....C.
 0030 065b0c07 08000000                    .[...... 
```

一：

64 位 Linux 可重定位目标文件（ELF 格式）

elf64 ：可执行链接格式



二：.text

左边：**地址偏移**。前十位16进制，表示当前行地址的下标偏移量字节

中间：**十六进制机器码**（CPU 真正执行的 0101）

这就是你的 `main` 函数编译后的**二进制机器码**。



三：.comment

编译器版本信息，只是标记用的，**没用**。



其他段（.eh_frame/.note）

都是给系统用的，**不用管**。



- objdump -h main

ELF 可执行文件的段表头（Section Headers）。



```
Idx Name      Size      VMA       LMA       File off  Algn

Idx：段编号
Name：段名字（.text/.data 等）
Size：段大小（字节）
VMA：虚拟内存地址（运行时的地址）
File off：在文件里的偏移位置
Algn：内存对齐
```



```
CONTENTS, ALLOC, LOAD, READONLY, CODE

CONTENTS：文件里真实存在数据
ALLOC：运行时需要分配内存
READONLY：只读
CODE：这是代码段（.text）
DATA：这是数据段（.data/.rodata）
```



#### 反汇编



```
objdump -d main.o
```



主要是将 .text 节的内容反汇编为汇编代码。



### 链接

链接阶段，由连接器完成。连接器将各个目标文件以及可能用到的库文件进行连接，生成最终的可执行程序。在这个阶段，连接器会解析目标文件中的符号引用，并将它们与符号定义进行匹配，以解决符号的地址关联问题。链接器还会处理全局变量的定义和声明，解决重定位问题，最终生成可执行文件或共享文件。



#### 链接方式

printf函数，来源于glibc库，printf的实现在glibc的二进制组件中，通常是在共享库（如libc.so）或者静态库（如libc.a）文件中。因此，我们处理要连接main.o、hello.o，还需要和 glibc 库的文件链接。通常，c语言的俩姐共有三种方式：静态链接、动态链接和混合链接。三者的区别就在于链接器在链接过程中对程序中库函数调用的解析。



#### 静态链接

将所有目标文件和所需的库在编译时一并打包进最终的可执行文件。库的代码被复制到最终的可执行文件中，使得可执行文件变得自包含，不需要再运行时查找或加载外部库。

```shell
gcc -static main.o hello.o -o main
```



- -static：指示链接器进行静态链接，而不是默认的动态链接。GCC会尝试将所有用到的库函数直接链接到最终生成的可执行文件中，包括C标准库（libc）、数学库（libm）和其他任何通过代码引用的外部库。





#### 动态链接

库再运行时被加载，可执行文件包含了需要加载的库的路径和符号信息。动态链接的可执行文件比静态链接的小，因为它们共享系统级的库代码。与静态链接不同，库代码不包含在可执行文件中。



##### 方式一

```shell
gcc main.o hello.c -o main
```



没有添加-static 关键字，gcc 默认执行动态链接，即glibc库文件没有包含到可执行文件中。



##### 方式二

将自己编写的部分代码处理为动态库。

将hello.o 编译为动态链接库 libhello.so

```
gcc -fPIC -shared -o libhello.so hello.o
```

- -fPIC : 这个选项告诉编译器为“位置无关代码（POsition Independent Code）”生成输出。在创建共享库时使用这个选项时非常重要的，因为它允许共享库被加载到内存中的任何位置，而不影响其执行。这是因为位置无关代码使用相对地址而非绝对地址进行数据访问和函数调用，使得库在被不同程序加载时能够灵活地映射到不同地地址空间。
- -shared：这个选项指示GCC生成一个共享库而不是一个可执行文件。共享库可以被多个程序同时使用，节省了内存和磁盘空间。

- -p libhello.so ： 这部分指定了输出文件地名称。命名为libhello.so。按照惯例，linux下地共享目录名称以lib开头，扩展名为.so（表示共享对象）
- hello.o ： 这是命令地输入文件，即之前编译的目标文件。



链接

```
gcc mian.o -L ./ -lhello -o main
```

-L : 目录

-l+名称

```
wm@asdf:/mnt/f/code/C/gcc$ ./main2
./main2: error while loading shared libraries: libhello.so: cannot open shared object file: No such file or directory
```



没有找到动态链接库文件。linux的默认动态链接库文件夹时/lib和/usr/lib

所以在执行时指名额外的动态链接库文件夹 

```
LD_LIBRARY_PATH=/home/atguigu/helloworld ./main
```

LD_LIBRARY_PATH = path

LD_LIBRARY_PATH=$PWD ./main2



#### 混合链接

某些库静态链接，而其他库动态链接。这种方式结合两种优点。

执行下面的指令可以将hello.o编译为静态链接库libhello.a

```
ar crv libhello.a hello.o
```

- ar : 归档命令，用于处理静态库文件
- crv ：ar命令选项，由三个字符组成，每一个字符表示一个选项：
    - c：创建归档文件。如果不存在ar创建
    - r：替换归档文件中现有的文件或者向归档文件中添加新文件。如果hello.o已经在libhello.a中，它会被新版本替换；如果不存在，则会被添加。
    - v ：详细模式（verbose mode），在处理文件时显示详细信息。使用这个选项，ar会列出它正在执行的操作，包括哪些文件被添加或替换。
- libhello.a ： 要创建或更新的静态库文件名称。按照惯例，Linux下的静态文件名以lib开头，并以.a作为扩展名。

同一个目录下，既有动态库又有静态库，优先使用动态库

```
gcc mian.o -L ./ -lhello -o main
```



#### glibc的动态库和静态库

glibc的动态库和静态库分别位于：/usr/lib/x86-64_64-linux-gnu/目录下的libc.so和libc.a文件中































