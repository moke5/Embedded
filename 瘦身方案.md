# 仓库瘦身方案：图片出库 + 历史重建

> 2026-09-02 制定。目标：远程仓库（GitHub + Gitee）从 317MB 降到 \~3MB 纯文本，推送秒级，图片策略一次配置永久生效。

## 一、摸底数据（方案依据）

| 项     | 数据                                         | 结论                        |
| :---- | :----------------------------------------- | :------------------------ |
| 仓库总体积 | 317MB，其中 `课程/` 317MB、`杂记/` 1.9MB、`项目/` \~0 | 体积问题在课程图片                 |
| 图片    | 2109 张共 316.3MB（平均 154KB），最大 GIF 单张 24MB   | 图片 = 体积的 99%              |
| 重复图片  | 24 组 / 29 文件 / 仅 4.2MB                     | 去重解决不了问题，本轮不做（下轮内容归并时处理）  |
| 远程状态  | origin 落后 19 个 commit（P0\~P5 + pcb 全部未推）   | **316MB 从未上传过远程**，历史重建零成本 |
| 纯文本   | 不到 3MB                                     | 重建后推送秒级                   |

## 二、决策记录（已拍板）

1. 远程仓库定位：**纯备份**（阅读全在本机 Obsidian/Typora）→ 图片无需进远程
2. 图片本身：**不做异地备份**，接受硬盘单点故障风险
3. 本轮范围：**只做体积治理**；内容归并（ESP32 两处、智慧农业两处、传感器分散、新目录无 MOC）下轮
4. 方案：**A 图片出库 + 历史重建**（否决 B 压缩保留：Gitee 500MB 红线迟早撞；否决 C git-lfs：双远程配置割裂）
5. 新历史：**单 commit**
6. 旧历史：**留兜底 tag**（本地 .git 保留约 300MB blob，可随时完整恢复）

## 三、核心设计

```
本地（完全不变）                          远程（重建后）
课程/STM32/笔记.md ←相对路径→ img/图片    课程/STM32/笔记.md（纯文本）
图片原位保留，Obsidian 照常显示            无图片（纯备份，已接受风险）
```

- `.gitignore` 追加：`**/img/` 与 `**/*.assets/`（覆盖 Typora 配对目录），与 README"图片统一放同级 img/"约定一致

- 磁盘图片一个字节不动、不删、不移动

- 未来 Typora 新插图落 `img/` → 自动被忽略，仓库永不因图片膨胀

## 四、执行步骤

```
1. 前置检查：工作区干净；确认 origin/main 位置；Obsidian 关闭（惯例）
2. git tag history-with-images            # 兜底 tag
3. .gitignore 追加两条图片规则
4. git reset --mixed origin/main          # 变化退回工作区
5. git add -A                              # gitignore 生效，2109 张图片自动排除
6. git commit -m "仓库瘦身：图片出库+目录重构（纯文本仓库）"
7. 验证：git ls-files 中图片数 = 0
8. git push origin main && git push gitee main
9. 验证推送数据量 < 5MB；本机 Obsidian 抽查带图笔记显示正常
```

原理：`reset --mixed` 后图片变为"未跟踪+被忽略"，`git add` 不会碰它们——316MB blob 从未进入新历史，远程零接收。

## 五、边界与回滚

- **回滚**：`git reset --hard history-with-images` 一条命令回到当前完整状态（含图片历史）

- **本地磁盘**：旧 blob 留在 `.git`（约 300MB）；90 天后删 tag + `git gc` 可回收，留着也不影响远程

- **推送前提**：GitHub token 轮换若未完成，push 前先处理（P0 遗留事项）

- **新机器 clone**：图片缺失、笔记裂链——纯备份定位下已接受

- **本地未跟踪的旧忽略项**（物联网通信技术.md 等）保持现状不动

## 六、验收标准

- [ ] `git ls-files` 过滤图片后缀计数 = 0

- [ ] push 成功，传输数据量 < 5MB

- [ ] Gitee/GitHub 仓库页面大小显示降至个位数 MB

- [ ] 本机 Obsidian/Typora 打开带图笔记（抽查 STM32、文件IO、硬件设计和焊接 各一）显示正常

- [ ] README 图片条目已改写为"图片存本地 img/，不入库"

## 七、收尾

- README"图片"约定更新；本方案文件执行完成后按 P5 惯例评估去留

- AI 仓库按 CLAUDE.md 三步流程归档：知识条目（gitignore 对已跟踪文件无效 + reset --mixed 重建历史手法）、会话记录、变化日志

## 八、下轮预告：内容归并（另立方案）

- ESP32 两处（`项目/比赛` vs `课程/ESP32`）→ 归并或互链

- 智慧农业两处（`硬件设计和焊接/智慧农业系统电路设计` vs `杂记/技术专题/STM32驱动分层设计`）→ 互链或归并

- 传感器主题分散（`模块/` vs `硬件设计和焊接/`）

- `硬件设计和焊接/`、`ESP32/` 补 MOC

- 24 组重复图片清理（4.2MB，顺带）

