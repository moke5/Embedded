- 问：老师，使用STM32CubeMx导出的代码是只能用于STM32，能够适用于其他芯片吗，例如GD32
- 答：不可以。



- 问：老师，不使用STM32CubeMx软件，可以自己写初始化硬件的代码吗？
- 答：可以的。使用STM32CubeMx有利于提高开发效率，缩短开发周期。



- 问：老师，手上没有STLINK仿真下载器，能够用JLINK、ULINK仿真下载器吗？
- 答：可以的。



- 问：老师，为什么连接上仿真下载器后，无法正常程序烧录？

- 答：请按以下可能存在的问题逐一检查：

    1）工程代码是否已经成功编译通过；

    2）检查JTAG/SWD接线是否正确；

    3）检查STM32芯片是否已经供电；

    4）检查MDK烧录方式指定的接口是否跟仿真下载接口吻合。如仿真下载接口为JTAG，则MDK烧录方式指定的接口为JTAG。



问：老师，开发板上的硬件如何对应上原理图？

答：

***原理图***

  元件位号，电路图中见到的诸如R1、R2……，C1、C2,……，U1、U2……电路图中每一个元件的位号必须独立，不可重复，并跟PCB的丝印层中的元件标号一一相对应。

***PCB***

  PCB丝印层，Overlay, 用于标明元件标号或说明，如R1、C1等便于装配。通常PCB板有丝印层（top/bottom overlay），其作用是：为方便电路的安装和维修,在印制板的上下两表面印上需要的标志及图案及文字符号,如元件标号,版本号,日期等信息。

![img](./img/wKgP3GIPVluAZ1rNAAKaP50YUvE450.png)
元件对应示意图

------

问：老师，关于GPIO引脚内部的上拉电阻、下拉电阻的使能，哪种情况下才需要用到？

答：

上拉电阻：将一个不确定的信号（高或低电平），通过一个电阻与电源VCC相连，固定在高电平。

下拉电阻：将一个不确定的信号（高或低电平），通过一个电阻与地GND相连，固定在低电平。

上拉增大电流，下拉电阻是用来吸收电流。

------

问：老师，关于GPIO引脚速度的设置多少比较合适？

答：
速度的配置，就是决定IO口驱动电路的响应速度。

我们需要结合实际情况配置速度，不同速度会有不同的影响。

功耗与干扰：

- 配置高速：输出频率高，噪音大，功耗高，电磁干扰强；
- 配置低速：输出频率低，噪音小，功耗低，电磁干扰弱；提高系统EMI（电磁干扰）性能；

  实际情况中，比如：低功耗的产品，你会考虑功耗。环境不好的场合，通信不稳定，你会考虑电磁干扰等。

速度配置实例：

  (1) 对于串口来说，加入最大波特率为115200bps，这样只需要用2MHz的GPIO的引脚速度就可以了，省电噪音又小；


  (2) 对于I2C接口，假如使用400KHz波特率，若想把余量留大一些，2MHz的GPIO引脚速度或许是不够，这时可以选用10MHz的GPIO引脚速度；


  (3) 对于SPI接口，假如使用18MHz或9MHz的波特率，用10MHz的GPIO口也不够用了，需要选择呢50MHz的GPIO引脚速度;


  (4) GPIO口设置为输入时，输出驱动电路与端口是断开的，所以这时配置输出速度是无意义的。

------

问：老师，做产品时，PLL频率一定要设置为180MHz？

答：可以根据产品实际的应用场景决定其运行频率。在功耗有要求的场景，可适当调低PLL输出频率。在对性能有要求的场景，可将PLL输出频率拉满。



问：老师，HSI振荡时钟可以作为PLL的时钟源吗？为什么一定要使用HSE振荡时钟。

答：
  HSE振荡时钟精度非常高，HSI振荡时钟在官方手册也有以下说明：

- HSI RC 振荡器的优点是成本较低（无需使用外部组件）。但即使校准后，其精度也不及外部晶振或陶瓷谐振器。
- 因为生产工艺不同，不同芯片的 RC 振荡器频率也不同，因此 ST 会对每个器件进行出厂校 准，达到 TA = 25 °C 时 1% 的精度。
- 如果应用受到电压或温度变化影响，则这可能也会影响到 RC 振荡器的速度。


问：老师，HSI振荡时钟具体什么使用？

答：

1. 当为了节省硬件成本，直接使用HSI振荡时钟作为PLL或系统时钟的输入时钟源。
2. HSI振荡时钟还可作为备份时钟源（辅助时钟）使用，以防 HSE振荡时钟发生故障。



问：老师，按键识别可以放在主函数检测，有必要使用中断吗？

答：主函数难以保证实时性，例如当前主函数在执行OLED屏显示，全屏显示需要1.5秒时间，则按键的识别就会延迟，导致错过按键的按压状态。



问：老师，中断优先级分组选择哪一组会比较好？

答：推荐选择第0组或第4组。其他组既包含中断抢占优先级，又包含子优先级，不易于理解和管理。

- 第0组，中断执行过程中不被其他中断抢占，保证原子执行。
- 第4组，低抢占优先级中断执行过程中被高抢占优先级中断剥夺其CPU使用权，提高后者实时性，但前者则被延后执行。常见场景就是电子设备关机按键。

可靠性：第0组 > 第4组。

实时性：第4组 > 第0组。

问：老师，为什么按下按键会偶尔触发不只一次？

答：键机械触点断开、闭合时，由于触点的弹性作用，按键开关不会马上稳定接通或一下子断开，使用按键时会产生如下图带波纹信号，需要用软件消抖处理滤波。

![img](./img/wKgP3GIdow-AYappAAAfn9EEcuo139.png)

### 定时器

问：老师，STM32已经有了系统定时器，为什么还需要其他外设定时器呢？

答：系统定时器主要用途是延时与硬件的同步，例如温湿度模块的数据采集常结合延时函数模拟时序图；外设定时器按需提供异步定时事件，提高实时性，如定时控制继电器、按键扫描等。



问：老师，在学习PWM章节时，外设定时器的计数值与比较值的作用是什么呢？

答：计数值决定TIM输出PWM的频率；比较值决定PWM的占空比。



### 串口通信

问：老师，为什么开发板已经连接了USB线，在电脑无法识别到串口？

答：排查问题如下：

（1）检查USB线是否已经正确连接到开发板的USB B-Type的母口座；
（2）检查USB线、电脑USB口是否存在质量问题；
（3）检查电脑是否已经安装了USB转串口驱动，默认是CH340 USB转串口。

问：老师，我想通过串口访问蓝牙模块、WiFi模块，拓展口在哪里位置？

答：

![img](./img/wKgP3GIpwsmATGhuAATGf94jG3Q506.png)

问：老师，为什么在串口调试助手中接收到的数据显示乱码？

答：检查串口代码配置的波特率是否与串口调试助手一致。



### 看门狗

问：老师，怎么知道当前复位是由喂狗超时导致的复位？
答：可以调用__HAL_RCC_GET_FLAG函数获取对应的复位标志是否成立，若成立则由该事件导致的复位,喂狗超时复位检测参考代码如下：

```
if(__HAL_RCC_GET_FLAG(RCC_FLAG_IWDGRST))
{
    printf("iwdg reset cpu\r\n\r\n");
}

if(__HAL_RCC_GET_FLAG(RCC_FLAG_WWDGRST))
{
    printf("wwdg reset cpu\r\n\r\n");
}
```



问：老师，独立看门狗的喂狗代码放在哪里位置会比较合适？

答：使用技巧如下：

- 无运行RTOS环境下，要放在定时器中断中喂狗，并在while(1)循环中添加计数型变量，监视主函数是否运行异常，若是则触发独立看门狗复位，否则继续正常喂狗。
- 有运行RTOS环境下，可以单独放到一个任务里喂狗。若当前任务运行异常，则触发独立看门狗复位，否则继续正常喂狗。

问：老师，独立看门狗与窗口看门狗两者区别是什么，各用在哪种场合？

答：IWDG 最适合应用于那些需要看门狗作为一个在主程序之外，能够完全独立工作，并且对时间精度要求较低的场合。WWDG 最适合那些要求看门狗在精确计时窗口起作用的应用程序。



### ADC

问：老师，为什么改变可调电阻，测量到的电压值没有发生变化？

答：排查问题如下：

（1）检查代码是否有问题。

（2）检查可调电阻是否存在硬件问题。

问：老师，ADC的分辨率与精度区别是什么？

答：

```
分辨率
```

分辨率（Resolution）是指ADC能够分辨量化的最小信号的能力，用二进制位数表示。

比如：一个10位的ADC，其所能分辨的最小量化电平为参考电平（满量程）的2的10次方分之一。即分辨率越高，就可以将满量程里的电平分出更多份数，得到的结果就越精确，得到的数字信号再用DAC转换回去后就越接近原来输入的模拟值。

所以，对于给定的一个具体ADC器件，其分辨率值是固定的。

```
精度
```

精度（Precision）是指对于给定模拟输入，实际数字输出与理论预期数字输出之间的接近度（误差值是多少）。换而言之，转换器的精度决定了数字输出代码中有多少个比特表示有关输入信号的有用信息。

有些ADC器件的datasheet中，会注明精度值或精度范围。

```
区别
```

分辨率和精度，不能混为一谈，其中“精度”是用来描述物理量的准确程度的，而“分辨率”是用来描述刻度划分的。

其实，对于ADC来说，这两样都是是非常重要的参数，往往也决定了芯片价格，显然，我们都清楚同一个系列，16位ADC一般比12位ADC价格贵，但同样是12位ADC，不同厂商间又以什么参数区分性能呢？性能往往决定价格，那么什么参数对价格影响较大呢？此时就要用精度来衡量。

引用一个别人的比喻：有这么一把常见的塑料尺（中学生用的那种），它的量程是10厘米，上面有100个刻度，最小能读出1毫米的有效值。那么我们就说这把尺子的分辨率是1毫米，或者量程的1%；而它的实际精度就不得而知了。

问：老师，当ADC采样更高的分辨率，如何让得到的数据更加精确？

答：

- 硬件设计

    输入的电源更加纯净；

    提高电路的抗干扰能力；

- 软件设计

    使用求平均值滤波算法。



### RTC

问：老师，使能闹钟A后，还可以使能闹钟B吗？

答：可以。

问：老师，RTC能够使用内部LSI吗？

答：可以，但不推荐，精度不高。

问：老师，为什么stm32复位后，时间会被重置呢？

答：使用RTC备份寄存器解决。



### IIC

问：老师，I2C常见的速率有哪些？

答：常见的有3中，分别为：100KHz（标速）、400KHz（快速）、高速（3.4MHz~）。



问：老师，I2C总线一定需要上拉电阻吗？为什么呢？

答：需要上拉电阻。原因如下：

- 防止短路；
- 可以实现“线与”逻辑，可以减少一个与门的使用，简化电路。

结论：I2C支持多个主设备与多个从设备连接在同一根总线上，如果不开漏输出，会容易出现短路现象。



### SPI

问：老师，SPI工作模式有多少种？

答：共4种。



问：老师，访问spi flash可以用工作模式1和工作模式2吗？

答：不可以，数据手册已规定好其规定的工作模式。



问：老师，访问spi工作速度能够随意设置吗？

答：不可以，不能超过spi从设备支持最大的速率。



问：老师，对spi flash能够直接写入数据吗？

答：不可以，首先要解除写保护，接着进行扇区擦除后才能写入数据。